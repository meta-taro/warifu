[package]
name = "warifu-calendar"
description = "予定表。**空いているかどうかだけを外に出す。**"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# **依存を持たない。**空き時間を出すのに、通信もモデルも要らない。
[dependencies]

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
