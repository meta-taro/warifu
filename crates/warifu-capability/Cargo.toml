[package]
name = "warifu-capability"
description = "何をしてよいかの判定。**モデルの外で決める。**"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# **依存を持たない。**判定にモデルもネットワークも要らない（issues/008）。
[dependencies]

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
