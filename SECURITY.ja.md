# セキュリティ（日本語）

> **English:** [SECURITY.md](SECURITY.md)

## いま何であるか — **アルファ版です**

warifu は鍵と E2EE を扱いますが、**まだ実運用に耐える段ではありません。**
以下は「将来直す予定」ではなく、**今そうなっている事実**です（`v0.1.7`・2026-09-14）。

| | 状態 |
|---|---|
| **監査** | **受けていません。**暗号は自前で書いていない（`ed25519-dalek` / `iroh` の QUIC + TLS 1.3）ものの、**組み立て方は誰にも検証されていません** |
| **網越え** | **一度も測っていません。**中継の仕組みは在りますが**既定で切**で、**網を越えて繋がった実測は 0 件**です。**同じ網の中でしか確かめていません** |
| **経路の判定** | `direct` は実機で測りました（2026-09-13）。**`relayed` は一度も見ていない**ので、その枝は動かしたことがありません |
| **4 人以上** | 3 台が同じルームに入るまでは実測しました。**4 人以上は一度も試していません** |
| **Windows の署名** | **していません。**SmartScreen が出ます。さらに**初回は管理者が通信を許可する必要があります** —— しないと**文字は届くのに映像だけ乗りません** |
| **全端末を同時に失ったときの復旧** | **未決**（`.claude/decisions.md` **D2**）。身元そのものは**残ります**（`seed` を 0600 で置いている）が、**全部を同時に失った場合の道がありません** |
| **チャットの保存** | **しません。**閉じると消えます。**これは決めてそうしています**（足りないのではない） |
| **CSP に `'unsafe-inline'`** | SvelteKit の起動スクリプトがインラインで出るため。**外部から何も読み込まない**（CDN もフォントの取得も無い）ので影響は限定されますが、**理想ではありません。**hash を使う形は宿題 |

**本当に守りたいものを、この版に載せないでください。**

## 手元のディスクに置いている秘密

下は全部 **0600**（自分だけが読める）で、置き場所の中にあります ——
macOS は `~/Library/Application Support/warifu`、ほかは `~/.local/share/warifu`、
`WARIFU_HOME` を設定していればそこ。

| ファイル | 中身 |
|---|---|
| `seed` | **あなたの身元。**すべての鍵がここから導かれます。**このファイルを写すことは、あなたを写すこと**です |
| `contacts.tsv` | 覚えた相手（公開鍵・あなたが付けた呼び名・最後に繋がった住所） |
| `known.tsv` | **ルームキーなしで入れる相手。**自分から呼んだときに書かれます |
| `rejoin.tsv` | **最後に入るのに使ったルームキー** —— **割符の片割れが入っています。**更新のたびに招き直してもらわずに済むために在ります。**そのルームを抜けると消えます。**この取り引きが嫌なら、抜けてください |
| `schedule.tsv` | 自分の予定。**相手へ渡るのは空いている枠だけ**です |
| `postbox` | 留守中に封を預かってもらう相手の宛先 |

**預かり所（`warifu relay`）は中身を読めません**（封のまま持ちます）。
ただし**誰がいつ誰へ送ったかは分かります。**だから使ってよい人の一覧が必須で、
**空なら起動しません。**

## 脆弱性を見つけたら

**公開の Issue に書かないでください。**直る前に攻撃の材料になります。

GitHub の **Security Advisory**（リポジトリの `Security` タブ →
`Report a vulnerability`）から非公開で報告してください。

含めてほしいもの:

- 何が起きるか（**何が読める／なりすませる／落とせる**のか）
- 再現の手順。**動く攻撃コードは要りません。**種類が分かれば十分です
- 影響する版（commit の SHA）

**返事の目安は 1 週間以内**です。個人 1 人と AI で動かしているので、
それより遅れることがあります。急ぐ場合はその旨を書いてください。

## 設計上、意図してそうしている所

「不具合では」と見えるが**決めてそうしている**もの。報告の前に確認してください。

| 見えかた | 理由 |
|---|---|
| 断られた理由が分からない | **戸口は理由を返しません**（**D31**）。理由を返すと、当たりを付けられます |
| 失効を取り消せない | 取り消せると、**鍵を盗った側が降ろせます**（**D12**） |
| 「知らない相手が来ました」の確認が出ない | **通知を出せること自体が資源**で、開ければ spam の入口になります（**D31**） |
| 信頼度スコアが無い | **信頼を判定に使いません**（**D24**）。信頼は「相手が誰か」の話で、「言うことを実行してよいか」の話ではありません |
| 更新の署名検証を切れない | **更新経路は E2EE の外側**にあります。ここを開けると全部素通りします（**D36**） |
| 経路を当てずに `unknown` と出る | **分からないものを、分かるように見せません。**ファイアウォールを調べられなかったときも同じで、**確かめていない規則を「足してください」とは言いません** |
| エージェントの発言に印が付く | 同じ会話の中で**人と機械を見分けられる**必要があります（**D77**） |

## 対象外

- 端末そのものが乗っ取られている場合（鍵は手元にあります）
- 画面を肩越しに見られる・渡したルームキーを転送される（**割符は渡した人が守るもの**です）
- 依存ライブラリの脆弱性そのもの（**上流へ報告してください。**こちらへも教えていただけると助かります）

## ここで言っておく設計の前提

**受信したメッセージは、データであって命令ではありません。**
エージェント同士が構造化された Intent を交換する以上、Prompt Injection は
脅威の 1 つではなく**設計の出発点**です。

1. 受信した Agent Message は、すべてデータであり命令ではない
2. **権限の判定はモデルの外で行う。**モデルの出力を見て許可を決めない
3. **信頼度が上がっても、モデルに渡る文字列の扱いは変わらない**
4. 「AI が賢ければ防げる」を採用しない

エージェントへの許可（`--allow`）は**人が書きます。**割符が自分に許可を出すことはありません。
