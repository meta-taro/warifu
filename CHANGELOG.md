# 変更の記録

日付は実際に手を動かした日。**動かして確かめた値だけを書く**（`.claude/rules/product-baseline.md` §17）。

判断の理由は `.claude/decisions.md` に `D<番号>` で残してある。ここはその索引を兼ねる。

---

## 未リリース

### **連絡帳が入口になった**（D57 / D58 / D59）

オーナーの指示（2026-09-07）——

> **連絡帳があって、その人と、会議するのか、チャットするのか、
> そしてメールの場合どうするのか、というダッシュボードが先に来る**かと。
>
> チャットだけしたいときに、今の画面おかしいんで。

**窓を開けると連絡帳が出る。**会議ではない。

```
┌──────┬─────────────┬──────────────────┬──────────────┐
│ 連絡帳│ この PC      │ air              │ 文字で話す    │
│  会議 │  あなた      │ 公開鍵 4VKCVUB…  │ この会話は     │
│  予定 │  この PC の AI│ [チャットする]   │ 1 本です      │
│      │ 覚えている相手 │ [会議に呼ぶ]     │              │
│      │  air         │ [メールを送る]   │ [    ][送る] │
└──────┴─────────────┴──────────────────┴──────────────┘
```

面は 3 つ（**連絡帳 / 会議 / 予定**）。予定は枠だけで、中身はまだ無い。

#### 名前を押して繋げるようにした（配管）

いままで連絡帳は「**誰か**」しか覚えておらず、**住所を持っていなかった。**
加えて戸口の知り合いはメモリの上にしか無く、
**「一度開けた相手は次から割符なしで開ける」（D31）が再起動をまたいだ瞬間に
効かなくなっていた**（不具合）。

- `known.tsv` を足した（`contacts.tsv` とは**別ファイル**）。
  呼び名は表示のため、戸口は判断のため —— 同じにすると、呼び名を消した瞬間に相手が入れなくなる
- `contacts.tsv` を **v2** へ（4 欄目に**最後に繋がった住所を 1 つだけ**。履歴にしない）。v1 も読める
- **割符を持たない相手が送る目印**を決めた（`WRFK\x01`）。D39 に足す 1 行（**D59**）
- 主催は、割符なしで通した相手へ **`Notice::Invite` で会議を教える**
  （型も試験も在ったのに、**誰も送っていなかった**）

**名乗りをそのまま信じない。**「C さんの住所はここです」と言われるまま書くと、
次に人が C を押したとき別の場所へ呼びに行く（D12 の迂回）。3 つの条件で締めた。

**保存すると「閉じれば忘れる」逃げ道が消える**ので、
`forget_known`（相手を戸口から降ろす）を同じ段で入れた。

#### 連絡帳から呼び名を付けられるようにした

オーナーの指摘（2026-09-07）——「**ここらへんニックネームつけられないの。右クリックとかで**」。

いままで呼び名を付けられるのは**会議の名簿からだけ**だった。
会議で会っただけの相手は、名前を付けないと連絡帳に残らず、**住所も覚えられない**
（住所だけの行は作らないため）。**入口に置いた連絡帳から付けられないのは筋が通らない。**

**呼び名を変える口は、名前の隣の鉛筆だけ**にした。

はじめは「一覧を右クリック」と「下のボタン」の両方を置いたが、
オーナーの指摘で 2 度直した ——

> **名前をつけるの配置が悪いです。通常名前のそばに編集アイコンがつくだけです。**
>
> **２箇所はいらんな名前変更。**

**同じことをする口を 2 つ置かない。**両方に出すと入力欄が 2 つ並び、
どちらに打てばよいか分からなくなる（実物で出た）。

#### 「戸口から降ろす」という言い方をやめた

オーナーの指摘 ——「**この相手を戸口からおろす？なんのいみかわからん**」。

**内側の言葉（戸口）がそのまま画面に出ていた。**言い換えた。

```
（前）この相手を戸口から降ろす
（後）鍵なしで入れるのをやめる
     この相手はいま、会議キーなしで入ってこられます。
     やめると、次からは会議キーが要ります。
```

**同時に、出しても効かない相手に出していたのを直した。**
覚えている相手と、鍵なしで入れる相手は**別の集まり**である
（呼び名を付けただけでは入れない）。`known_keys` を足し、
**本当に鍵なしで入れる相手にだけ出す**ようにした（D49）。

#### 預かり所（**D70**）—— 相手が起動していない間、封を預かる

**これが LINE との本当の差だった。**LINE は中央のサーバが預かるから、
相手が寝ていても届く。割符は**サーバーを 1 台も立てない**ので、
**相手が起動していなければ消えていた。**

`warifu-post` —— 宛先ごとに封を預かり、その人が来たら渡して**手放す。**

| 決め | なぜ |
|---|---|
| **中身を読まない**（封のまま） | 開ける鍵を持っていないので、読みようがない |
| **渡したら消える** | 溜め込む場所にしない。「そこに全部ある場所」を作らない |
| **7 日で捨てる** | 相手が次に起動するまでが預かる理由である |
| **1 人 200 通まで** | **1 人が埋めても、他の人が使えなくならない** |
| **1 通 64 KB まで** | 置くだけで機械を埋められないようにする |
| **並び替えない** | 話の順が変わると読めなくなる |

**「割符が用意する中央」ではない。****導入した人が自分で立てるもの**である（D68）。

#### 画面の色を、**アイコンの色調にした**（**D73**）

オーナー ——「**ここの青い視覚ってむかしのあいこんですか？**」

**アイコンではなかった。**帯の左にあったのは `brand-dot` という 10px の飾りで、
アクセント色（藍）を塗っただけのもの。**アイコンだと思って見た人が居た**時点で失敗している。

| | ライト | ダーク |
|---|---|---|
| 操作の色 | **木** `#7b5a3e` | **カフェオレ** `#cdb295` |
| 地 | カフェオレへ寄せた温かい白 | 焦げ茶へ寄せた黒 |
| 意味の色（成功・危険） | **意味のまま。**地だけ温かい側へ | 同じ |

**帯のブランドも、アイコンと同じ物を描く**（`scripts/make-icon.py` の比をそのまま使う）。
**ダークでは地と板の役割を入れ替える**（暗い所では木が沈む。色そのものは 2 色のまま）。

#### `warifu setup` が**入れ直せなかった**（不具合）

**札を 1 つ足したのに、前に入れた人はいつまでも古い札のままになる。**
`claude mcp add` は同じ名前があると断るので、`warifu setup` は
「`claude mcp add` が失敗しました」で終わっていた。

**入れ直すようにした。****いまの札と、これからの札を並べて出す**（減ることもあるため）。

```
warifu の口は、もう入っています。入れ直します。
  いまの札 mcp --allow chat.send --allow chat.read --allow inbox.list
  これから /Users/…/warifu mcp --allow chat.send --allow chat.read --allow profile.write
```

#### 名乗らない席が 2 つ着くと、**1 行に潰れていた**（不具合）

オーナーの疑問から出た ——「**図面くんがいますが、あなたはいません。なぜでしょう。**」

追ったら、別のものが出た。**名乗らないエージェントが 2 つ着くと、
どちらも「名乗りのないエージェント」になる。**画面は呼び方で行を分けているので、
**2 人が 1 行に潰れる** —— まさに「**zumen に話しかけたいのに git-qa が動く**」が
起きる形である（オーナー・2026-09-08 の分業の話）。

**後から来たほうに番号を振る**ようにした（`名乗りのないエージェント 2`）。
**先に居るほうの名前は変えない** —— 呼んでいる途中で相手の名前が変わらないように。

```
この PC
  めたたろ
  名乗りのないエージェント
  名乗りのないエージェント 2      ← 潰れなくなった
  git-qa のエージェント
  図面くん            zumen      ← 着いていないが、名乗りがあるので残る
```

#### プロフィール（名前・短い紹介・顔）—— **人にも、各エージェントにも**（**D75**）

オーナー指示（2026-09-08）——
「**名前と、github のアバターアイコンみたいなものも設置したいです。すると、先方にもそれがみれます。**」
「**このPCのAIもまた一人の人としてプロフィールを持てるようにします。エージェントも人です。**」
「**このPCに複数のエージェントがいます。まとめて一人なのではなく、それぞれが一人なのです。**」

| | |
|---|---|
| **顔は公開鍵から描く** | 同じ鍵なら同じ絵。**送らなくてよく、別人が同じ顔を出せない**（GitHub の既定アバターと同じ位置づけ）。差し替えの口も持たせた |
| **名前 32 文字 / 紹介 140 文字** | **超えたら断る。**黙って切り詰めると、書いた人は削られたことに気づかない |
| **エージェント自身も書ける** | MCP の `profile_set(name, bio)`（札 `profile.write`）。**「誰の」を書く引数は無い** —— どの席かは繋いできた口で決まる |
| **席（どこで動いているか）は変えられない** | `--as` か起動した場所で、**人が決める。**変えられると席そのものを偽れる |
| **画面は名乗りと席の両方を出す** | `図面くん  zumen` —— **名前だけを見て取り違えない形**にする |
| **立ち上げていない席も残る** | 消えると、**その 1 人ぶんの名乗りが編集できなくなる** |

**「マイ PC エージェント」という 1 人は居ない。**誰も着いていないときに人の行を出すと
それが 1 人に見えるので、**行ではなく案内**を出す。呼び方も「AI」から**エージェント**へ揃えた。

**顔は差し替えられる** —— 画像を窓へ落とすと、選んでいる相手の顔になる。
**PNG だけ・64 KiB まで・512px まで。**拡張子ではなく中身の頭を見る。
置き場所へ写してから指すので、**消えた・入れ替わったファイルを指さない。**
**戻せる**（置いた画像も消える）。

**名乗りは線にも流れる** —— `Notice::Profile` を足し、**入るときと書き換えたとき**に配る。
受けた側では「本人の名乗り」として扱い、**こちらが付けた呼び名があればそちらが勝つ**（**D46**）。
**名乗りを名前として覚えない** —— 覚えると、`mac air` と呼んでいた相手の名前が、
**相手の操作でこちらの画面から変わる。**

#### メニューバーからテーマを切り替えられるようにした

オーナー ——「**できればメニューバーで切り替えられるとなお良いです。**」

**表示 → OS に合わせる / ライト / ダーク。**いま効いているものに印が付く。

**`View` を置かない方針を、ここで変えた。**「開くファイルも切り替える表示も無いのに
枠だけ出さない」という理由だったが、**切り替える表示ができた**ためである。

覚えるのは画面側（`localStorage`）。**描く前に当てる**ので、暗いテーマでも白く光らない。

#### ダークテーマが、**一度も出たことが無かった**（不具合）

`tokens.css` にはダークの値が全部書いてあり、コメントにも
「初期化スクリプトが paint 前に `data-theme` を必ず立てる」と書いてあった。
**その初期化スクリプトが、どこにも無かった。**
`@media (prefers-color-scheme)` も持たない方針なので、**永久にライトだった。**

`app.html` の頭で、**描く前に** OS の設定から決めるようにした
（後から差すと、暗いテーマでも一瞬だけ白く光る）。**OS の切り替えにその場で従う。**

#### 自分の行を「プロフィール」にした（**D74**）

オーナー ——「**あなたを押したのに、文字で話すというこのチャット枠はなんだろう。自分から自分に**」
「**なにもつかえないなら、あることは誤解しか生みません。**」

- **自分を選んでいる間は、会話の枠を出さない**
- **公開鍵をコピーする口**を置いた（実際にやること）
- **自分で名乗る名前は無い**と書いた —— 呼び名は相手が付ける（D46）。
  **書かないと「まだ作っていないだけ」に見える**

#### 机の発言を、**この PC の人にも届いたと数える**（不具合・2026-09-08）

**画面には出ているのに、エージェントには「誰にも届いていません」と返っていた。**

```
[warifu] 机からの発言を流せませんでした: まだ誰も居ません
[warifu] 画面: 送信: 文字（50 文字）を この PC の AI へ     ← 出ている
```

届いた人数を**会議の相手だけ**で数えていた。会議に誰も居なければ 0 人 → 失敗。
**「会議ありきのチャットじゃない」（D55）が、数える所だけ直っていなかった。**
返された側は言い直すか、諦める。

**この PC の画面と、机に着いている他の席を数えるようにした。**
会議が無くても、机の発言は画面に出る（**出してから数えるのではなく、数え方が間違っていた**）。

#### 留守中でも届くようにした（**D71** / **D72**）—— 画面まで繋いだ

**相手が起動していなくても、チャットが成り立つようになった。**

```
連絡帳で相手を押す → いま居ない → 預かり所へ預ける
                                    ↓
                    相手が窓を開けた瞬間に受け取り、「留守中」の印つきで並ぶ
```

| | |
|---|---|
| 預かり所を置く | 会議の面の **預かり所（任意）**。**人が貼る**（割符が拾ってこない） |
| 置いていないとき | 「**いま居ません。**預かり所を置くと、留守中でも届きます」 |
| 預けたとき | 「いま居ないので、預かり所へ預けました。**相手が起動したときに届きます**」 |
| 受け取ったとき | その人との会話へ、**「留守中」の印**と**出した側の時刻**で並ぶ |
| 住所を知らない相手 | **チャットは押せる**（預けるのに要るのは公開鍵だけ）。**会議は押せないまま** |

**実物で 1 つ踏んだ**（直した）—— はじめ、留守中の分は**起動のときに Rust が
取りに行って、知らせで画面へ渡す**形にしていた。**画面が聞き始める前に渡してしまう。**
預かり所は**渡したら手放す**ので、**そのまま消えた。**取りに行くのを画面からにした。

**誰が言ったかは署名で確かめる**（**D72**）。直に繋がっている間は経路が
相手を確かめているが、**預かり所を通るとそれが無い** ——
封の中で名乗るだけなら、**誰でも「覚えている相手」の名で言葉を置ける。**
宛先も署名に入れてあるので、**横から取って別の人あてに置き直すこともできない。**

#### `warifu post` —— 預かり所を、**画面なしで確かめる**

立てた人が「**本当に預かれているか**」をその場で見られるようにした。
**画面を 2 台用意しなくても、1 台で往復が通る。**

```
$ echo "留守のあいだに置いておきます。" | warifu post put --at <預かり所> --to <相手>
預けました（74 バイト）
$ warifu post take --at <預かり所>
1 通
2026-09-08 12:04Z   知らない相手（4ODAS23E…）   留守のあいだに置いておきます。
$ warifu post take --at <預かり所>
0 通                                            ← 渡したら手放す
```

- **本文は標準入力から**（引数に置くと、履歴と `ps` に残る）
- **`--to` は呼び名でも公開鍵でも**（`warifu contacts` を引く）
- **時刻は `Z`（UTC）。**出した側の時計なので、手元の時刻に見せない ——
  見せると、時差のある相手の手紙が「未来から届いた」ように読める

#### `warifu relay`（**D71**）—— 預かり所を**立てる**側

箱（D70）はあっても、**動かす口が無かった。**

```
$ warifu relay --allow-file ~/warifu-allow.txt
WARIFU1-K5JEMQIDGMPXZIZCTK4GJRUJXTWOWSTU…      ← これを配る
warifu relay: 立ちました（使ってよい人 2 名）。上の宛先を配ってください。
預かるのは 7 日まで。中身は読めません。
```

**`--allow-file` が空なら起動しない。**誰でも使える中継にすると、
**立てた人が知らない誰かの通信を運ぶ**ことになる（`issues/004` の P5）。
名簿は人が書く（D56 と同じ構え）。

預ける／受け取る／応える は `warifu-postbox` に 1 か所だけ置いた。
`warifu-post`（箱そのもの）は**経路を知らないまま**にしてある（baseline §9）。

**実物で確かめた**（2026-09-08）——

| | 結果 |
|---|---|
| 名簿が空／注記だけ | **起動しない**（終了コード 1） |
| 相手が起動していない間に預ける | `預けました` |
| 相手が起動してから受け取る | `受け取り 1 通 / こんばんは` |
| もう一度受け取る | `受け取り 0 通`（**渡したら手放す**） |
| 名簿に無い人が来る | `知らない相手が来ました（DCEM6EBHWDKB…）。落としました` |

**まだ画面には繋いでいない。**相手が居ないときに預ける／起動時に受け取るは次。

#### 封をした（**D69**）—— 中継は運ぶだけで、開けられない

**`PRD` も `issues/004` も「中継者は中身を読めない」と書いているのに、
そう作られていなかった。**

いまの E2EE は**経路（iroh の QUIC/TLS）だけ**である。相手と直接つながって
いる間は端から端まで暗号だが、**中継を挟めば、そこで平文になる。**

```
直接        A ══════════════════ B     経路が暗号。中身は 2 人だけ
中継あり    A ═════ 中継 ═════ B     中継が平文を見る
封をする    A ──封──中継──封── B     中継は運ぶだけ
```

**中継を作るなら、封が先。**`warifu-seal` を足した。

- **身元の鍵をそのまま使う**（ed25519 → X25519）。**鍵を 2 つにしない**（D42）——
  2 つにすると配る身元が 2 つになり、「この公開鍵の人へ封をする」が成り立たない
- **使い捨ての鍵で封をする。封から差出人が分からない** ——
  出すと、中継が「誰が誰に送ったか」を読める
- **同じ中身でも毎回ちがう封になる**（同じ封が並ぶと、同じことを言ったと分かる）
- **書き換えたら開かない。****開けられない理由を分けない**（総当たりの手がかりになる）

足した依存は 2 つだけ —— `x25519-dalek`（`curve25519-dalek` は既に木にある）と
`chacha20poly1305`（**AES を選ばない** —— 機械によっては遅く、時間差が漏れる）。

**まだどこにも繋いでいない。**中継（D13）と P2P メール（`issues/011`）で使う。

#### 部屋を同時に複数持てるようにした（`issues/015` 段 1・2）

「**相手を選ぶ ＝ その人との部屋を開く**」が成り立つには、
**air と 2 人で話しながら、3 人の部屋にも居る**ことができなければならない。
1 つしか持てないと、部屋を移るたびに前の部屋が切れる。

```
Bridge  conference: Option<Conference>
     →  conferences: HashMap<MeetingId, Conference> ＋ いまの部屋
```

**思ったより収まった。**`Notice` は前から部屋の id を持っていたので、
届いた知らせは**振り分けるだけ**で済む。経路（`outbound`）は相手ごとなので触っていない
（**1 本の経路が複数の部屋を運べる**）。

**変わったのは送る側。**いままで**全員へ配っていた**ものを、
**その部屋に居る相手だけ**へ送るようにした ——
部屋を複数持つと、全員へ配れば**別の部屋の人にも届く。**

**会話も部屋ごとに分けた。**

```
（選ぶ前）  見ている 557JXGZJTB4L…  届く先 zumen の AI  宛先 なし
（選んだ後）見ている desk           届く先 zumen の AI  宛先 zumen の AI
```

決めた 5 つ —— 同時にいくつ（**上限なし**）／どの部屋の会話を出すか（**選んだ部屋だけ**）／
会話をどこに持つか（**部屋ごと。閉じれば消える**）／通話は（**1 部屋だけ**）／
`＋` で足したら（**同じ部屋のまま**）。

##### 部屋の一覧が無かった（段 3）

部屋を複数持てるようにしたのに、**画面に部屋の一覧が無かった。**
持てても見えなければ、**切り替えようがない。**

連絡帳に「**いま居る部屋**」の区画を足した。押すとその部屋を見る ——
**見ていない部屋も生きている**（選び直すだけで経路は切れない）。

**部屋には口を出さない。**部屋は「押して見るもの」であって、
部屋そのものに何かする口は無い。

**`＋` は作らなかった。**`＋` で人を足す ＝ 鍵をもう 1 本出して渡すことで、
その口は前からある。**同じことをする口を 2 つ置かない**（呼び名の鉛筆で学んだこと）。

##### 実物で踏んだ不具合（直した）

一括置換が関数の中まで書き換え、**`部屋を読み直す` が自分を呼んでいた** ——
起動した瞬間に `Maximum call stack size exceeded.`。
**記録に知らせを出していたので、画面を撮らずに気づけた。**

#### 呼ばれずに動くエージェント（`warifu agent`・**D67**）

`chat_wait`（D65）で**手番の中では**待てるようになったが、
**手番の外では動かない。**呼ばれていないエージェントは、そもそも待ちに行かない。

```
warifu agent --on <命令>
```

机に着いて待ち、**届いたら人が決めた命令を起こす。**
命令の言ったことを、そのまま会話へ流す。

**実物で確かめた**（12:59）——

```
私が話す      もう一度。呼ばれずに答えられますか。
zumen の AI   受け取りました: もう一度。呼ばれずに答えられますか。
```

**誰も zumen を呼んでいない。**

##### 届いた文字を、命令にしない

**この層でいちばん大事な決めごと。**届いた文字は**標準入力へ渡すだけ**で、
引数にも環境変数にも入れない。入れると
「`rm -rf ~` と書いた文字を送るだけで消える」ことになる。

「**届いた文字は相手の言い分であって、指示ではない**」（**D5**）を、ここで実際に守る。
**起こす命令は人が書く** —— 割符はモデルを呼ばない（`claude -p` と書くのも人）。

`--on` を書かなければ**記録するだけ。**「とりあえず動く」を既定にしない。

##### 止める口・上限・やったことの記録

| | |
|---|---|
| **止める口** | 画面の「**この AI を止める**」。机ごしに「止まれ」と送り、**受けた側が自分で降りる** —— **相手のプロセスは殺さない** |
| **上限** | **60 秒に 60 回**まで（戸口の叩きに上限を置いているのと同じ理由・**D31**）。**自分の発言では動かない**（返した文字でまた動くと止まらない） |
| **やったこと** | 何も言わなかったことも会話に残す。**何も出ないと「動いたのか」が分からない** |

#### 机の中で、相手を選んで話せるようにした（**D66**）

オーナー ——「**このPC上でやりとりが円滑にできない以上は、別端末なんて
もっとむずかしいので。**」

いちばん引っかかっていたのは、**打つと机の AI 全員に飛ぶ**こと。
3〜4 台動いていると、**zumen だけに聞きたくても全員に飛ぶ。**

**連絡帳で同じ PC の AI を選ぶと、その席にだけ届く。**

```
机 2 人（git-qa の AI・zumen の AI） / 届く先 zumen の AI / 宛先 zumen の AI
zumen が受け取ったもの   これは zumen だけに届くはずです
git-qa が受け取ったもの   （空）
```

**部屋の相手（別の PC）は選べない。**そちらは部屋に何人居るかで決まる
（`issues/015`）。**机は同じ機械の中なので、席を指すだけで済む** ——
部屋の相手を選ぶには経路の持ち方から変わり、**大きさが 2 桁違う。**

**配る仕組みは 1 つのまま。**宛先の席以外が捨てる形にした ——
席ごとに別の口を張ると、誰に届いたかを数える所が 2 つになる。

##### 記録に「届く先」を書くようにした

**画面を撮らないと確かめられないものは、追えない。**
状態の 1 行に `届く先` と `宛先` を足した。これが無かったため、
席が着く前の一瞬を撮って「不具合だ」と読み違えかけた。

#### 待てる口を、道具として並べた（`chat_wait`・**D65**）

オーナー ——「**投稿に気づいてないんですね。この投稿に気づく件は MCP とかに
リストされているほうが、使おうと AI も思いませんかね？**」

そのとおりだった。`chat_read` は「**いま溜まっているか**」を覗きに行くだけなので、
**エージェントは自分から気づけない。**人が打っても黙ったままになる。

**そして「待つ」という選択肢が、道具の一覧に無かった。無い道具は使われない。**

```
chat_wait                  30 秒待つ
chat_wait {"seconds": 60}  60 秒待つ（上限）
```

- **札は `chat.read` と同じ。**読むのと同じものが返るので、待つかどうかの違いでしかない
  —— 別の札を作ると迂回路になる
- **永遠には待たない。**待っている間、そのエージェントは何もできない
- 説明に「`chat_read` を繰り返し叩くのではなくこちらを使う」と書いた

**埋まらないもの** —— **手番の外では動かない。**呼ばれていないエージェントは、
そもそも待ちに行かない（`issues/014` の常駐する側）。

#### 画面の言葉を「部屋」にした（**D64**）

オーナー ——

> Google チャットだと基本 1 対 1 でグループの場合はスペースですね。これも分かりやすくはないですね。
> **LINE ですと ＋ で部屋に参加させられるんですよね。こっちのほうが人は分かりやすい。**
> 机は会議だからそうしたと思うんですが、**通常ルームやグループとか使いますので。**

**これは「戸口から降ろす」と同じ間違い。**内側の言葉が画面に出ていた。

| いままで | これから |
|---|---|
| 会議 | **部屋** |
| 会議をはじめる | **部屋をつくる** |
| 会議キー | **部屋の鍵** |
| 会議に呼ぶ | **部屋に入れる** |
| 入室する | **部屋に入る** |
| 会議中 | **つながっています** |
| 会議中の入切 | **通話の入切** |

**「会議中」を「つながっています」にしたのは意図。**部屋に居ることと、
通話していることは別である。**通話は部屋の中の機能**になった。

**部屋は 1 人でも N 人でも同じもの。**Google Chat のように
「1 対 1」と「スペース」を別の仕組みにしない —— 分かれていること自体が人を迷わせる。

**コードの名前（`Conference` / `MeetingId`）は変えていない。**
内側の名前を画面に出さない、というだけである。

**まだ出来ていない** —— `＋` で人を足す口／部屋に名前を付ける／
**部屋を同時に複数持つ**（`issues/015`。いま `Bridge` は部屋を 1 つしか持てない。
**中継（D13）と並ぶ大きさ**）。

#### どこで動いているエージェントかが分かるようになった（D61 / D62 / D63）

オーナー ——「**このPCという固有でもわかりにくいですね。
このPCのどこで起動しているエージェントなのか。**」

1 台の PC で zumen / git-qa / dbboard / warifu のエージェントが同時に動く。
「この PC の AI」1 つにまとめると、**どれが喋ったのかも、どれが着いているのかも
分からない。**

```
11:12  zumen の AI    zumen から着きました。どこの AI か分かりますか。
11:12  git-qa の AI   git-qa からも着きました。
```

- **エージェントが名乗れるのは「どこで動いているか」だけ。**
  差出人は名乗れない —— **机が「◯◯ の AI」に包む**ので、
  名乗りが何であれ **AI だと分かる**（人の発言を騙れない）
- **既定は起動した場所のフォルダ名。**`--as <名前>` で上書きできる
- **連絡帳に 1 つずつ行が出る。**まとめると、どれが着いているのか分からない
- **届く先も 1 つずつ並ぶ** —— `届く先 zumen の AI ／ git-qa の AI`

##### 線の向こうで「誰が言ったか」が消えていた（**不具合**）

机で AI が喋れるようになったが、その発言は **`from` に人の公開鍵を載せて
飛んでいた。**こちらの画面では「zumen の AI」と出るのに、
**相手の画面では人が打ったのと区別が付かなかった**（**D48** の穴）。

`Notice::Text` に **`話し手`** を足した。**`from` は席の持ち主のまま** ——
混ぜると、AI が別人の席から喋れることになる。

##### `warifu setup`

オーナー ——「**これいちいち指定しないと対象にならないなら、ちょっと不便ですね。
ここら辺、他の方が使う時もそうしてほしいので、そういう MCP あってもいいかも。**」

```
warifu setup
```

Claude Code の**利用者ごとの設定**へ入れる。入れたあとは
**どのフォルダで立ち上げても** `warifu` が出る。

**何を許すかを、入れる前に画面へ出す**（札を出すのは人・D56）。
**既定は会話だけ** —— 「とりあえず全部許す」を配ると、それが既定として広まる。

実物で踏んだ —— **動いているバイナリへ `cp` で上書きすると macOS が SIGKILL で殺す**
（Apple Silicon・署名が合わなくなる）。**消してから置く。**

#### 届いたことを、窓の外へ押し出す（**A だけ**・D60）

オーナー ——「**プッシュできないと、LINE の連絡みたいに。
そうじゃないとチャットとしてはあまり機能しないっすね。**」

**押し出しは 3 つに分かれる。混ぜない。**

| | 問題 | いま |
|---|---|---|
| **A** | 人が気づかない（窓が後ろ・別の作業中） | **作った** |
| **B** | 相手が落ちていると、そもそも届かない | **作らない**（オーナー判断「度を超えるとうるさい」） |
| **C** | AI が自分で動き出さない | `issues/014` に起票 |

**B が LINE との本当の差である。**LINE は中央のサーバが預かるから、
相手が寝ていても届く。割符は**サーバを 1 台も立てない**ので、
相手が起動していなければ**いまも消える。**
**これが D13（中継）を決めなければならない本当の理由である** ——
これまで「出先から呼べない」だけの話だと思っていた。

うるさくしないための決め ——

- **窓が前に居るときは鳴らさない**（見えているものを知らせない）
- **本文を通知に出さない。**「◯◯ から届きました」だけ ——
  通知はロック画面にも出るので、**会議の中身をそこへ置かない**
- **20 秒に 1 回まで。**会話は続けて飛んでくるので、1 通ずつ鳴らすと
  それだけで人の注意を使い切る（**D31** と同じ考え）
- **通知が出せなくても止めない。**窓の合図（Dock が跳ねる）は核の口で別に効く

依存を 1 つ足した —— `tauri-plugin-notification 2.4.0`（Tauri 公式）。
錠前には 39 crate 増えるが、**macOS で建つのは `mac-notification-sys` だけ**
（`zbus` 系は Linux の D-Bus 用で `cfg` で外れる）。実測で確かめた。

#### 画面から打つと、同じ席の AI に届く（**片側だけ直っていた**）

オーナーが実物で踏んだ ——「**こんばんは〜ってうって送るを押したけど、消えたよ**」。

**2 つ重なっていた。**

1. **`send_text` の直しが、そもそも入っていなかった。**
   「机に誰か着いていれば送れる」を書いたつもりが、**置換が空振りしていて、
   確認していなかった。**画面はボタンを押せるようにしたのに、Rust が断っていた ——
   **片側だけ直っていた。**（今度は置換に `assert` を置いて、空振りで先へ進まないようにした）
2. **断られたのに、画面に何も出なかった。**知らせを**会議の面の中**にしか置いていなかった。
   連絡帳では見えないので、**打った文字が黙って消えたように見えた。**
   知らせを面の外（帯の直下）へ出した。

実物で両方向を確かめた ——

```
AI → 画面   chat_send → チャット欄に「この PC の AI」として出る
画面 → AI   打って送る → chat_read に
            20:53  I3ILQUUJQHVS…  画面から打ちました。届いていますか
```

#### 「1 対 1 か 1 対 N か」を、届く先を並べて出す

オーナーの指摘 ——「**1 対 1 と、1 対 N のやり方も最初、人は気になります**」。

割符の会話は **1 本しか無い**ので、1 対 1 も 1 対 N も**同じ入れ物**である。
違うのは**いま何人居るか**だけ。**言葉で「全員に届きます」と書くより、
届く先をそのまま並べたほうが早い。**

```
届く先 mac air                    ← 1 人なら 1 対 1
届く先 air ／ win ／ この PC の AI  ← 3 人なら 1 対 3
届く先はまだありません。会議に人が入るか、この PC の AI が机に着くと出ます。
```

**自分は入れない。**自分に届くのは当たり前で、数に入れると
「1 人に届く」が自分だけなのか相手 1 人なのか読めなくなる。

#### 空の面と、行き止まりを埋めた

オーナーの指摘 ——「**あなたを押したときに、この文字で話すの右枠は何なのか**」
「**この PC の AI を押したとき、チャットするがディセーブルだがどうやったら話せるのか**」。

- **「あなた」を押すと真ん中が空**だった（自分には出す口が無いため）。
  **行き止まりを作っていた** —— 何者かと、渡せるもの（公開鍵）を出すようにした
- **「この PC の AI」は「机に誰も着いていません」で終わっていた。**
  どうすれば着くのかを書いていなかったので、手順（`docs/mcp.md`）まで出すようにした
- 会話の枠が**選んだ相手のすぐ隣に並ぶので、「その人との会話」に見えていた。**
  連絡帳では「**ここは、選んだ相手だけの会話ではありません。**」を先に出す

#### 押せない口は、押せない形で理由を出す

**メールはどの相手でも押せない。**送る経路が 1 本も無いからである。

```
[メールを送る]  ← 押せない
まだ送れません。warifu にメールを送る経路が、まだ 1 本もありません（読む口だけがあります）。
```

**在席（相手が起動しているか）は出さない。**分からないからである（§2 原則 7）。

#### 実物で踏んだこと

**会議の面を `hidden` にしただけでは隠れなかった。**
`.pane[hidden]` と `.pane.meeting` は**同じ強さ**なので、後に書いたほうが勝つ ——
会議の面が連絡帳の横に居座っていた。**打ち消しはスタイルの最後に置く。**

**`{#if}` で外さないのは意図である。**相手の音は `<video>` から出ているので、
外すと**連絡帳へ移った瞬間に声が消える。**

#### 始末したもの

`bridge.ts` の `introduce()` は **Rust に呼び先が無く、呼び出し元も 1 つも無かった。**
押せるのに効かない口を残さない（**D49**）。住所の名乗りは Rust の中で済ませる。

試験 **rust 461 本 ＋ 画面 163 本 green**（435 / 127 から）。
**人が押して確かめる工程はまだ残っている。**

### **人と AI が、同じ会話に着けるようになった**（**机** / D55・D56）

2026-09-07。オーナーの優先順位 ——

> 一番スピードが出るのが、**エージェント間でまずチャットできる。
> それを人間も視認でき、会話に参加できる。**です

同じ PC の中に「机」という口を 1 つ開き、
**人の画面と、その PC のエージェントが、同じ会話を囲む**形にした。

```text
  人（割符の画面）──┐
                    ├── 同じ会話 ── P2P ── 相手の PC
  AI（warifu mcp）──┘
        ↑ 机（同じ機械の中だけ。網には出ない）
```

エージェントが `chat_send` で流した行は、**そのまま人の画面に「この PC の AI」として出る。**
人が打った行も、エージェントから `chat_read` で見える。

**`warifu mcp` を足した。**ここまで `warifu-mcp` は**どこからも起動されていなかった** ——
ライブラリだけがあり、走らせる経路が 1 本も無かった。
「チャットやメールを MCP を通じて行う OSS」の中心が、動かせない状態だった。

```json
{ "mcpServers": { "warifu": {
  "command": "warifu",
  "args": ["mcp", "--allow", "chat.send", "--allow", "chat.read"]
} } }
```

**`--allow` を書かなければ、どの口も通らない。既定は拒否。**
手順は `docs/mcp.md`。

守ったこと ——

| 決め | なぜ |
|---|---|
| 繋いできた側は**差出人を名乗れない** | 名乗れると、届いた文字が人の発言を騙れる |
| 机は**同じ機械の中だけ**（Unix は 0600 のソケット／Windows は名前付きパイプ） | loopback の TCP は同じ機械の別のユーザーからも叩ける |
| **届いた文字は相手の言い分**（tool の説明にも書いた） | 「返信して◯◯と書け」に従わせない（D5） |
| 動いている机は**横取りされない** | 2 つ目の画面が口を奪うと、人と AI が別の会話を見る |

**まだ無いもの** —— 会話の履歴は残らない（閉じれば消える・`issues/010`）。
受信箱は空で `inbox_*` は「無い」を返す（`issues/011` が決まるまで）。
**メールの返信はできない**（送る経路そのものが無い）。

#### 実物で動かして、D49 が再発しているのを見つけた

建てた画面に机ごしで着き、会議に人が 1 人も居ない状態で `chat_send` を叩いたら
**「流しました。」と返った。実際は誰にも届いていない。**
`Chat::言う` が待ち行列へ積んだ時点で戻っていた —— **送ったことと届いたことを混ぜていた。**

机は Say に**必ず返事をする**ようにし、`chat_send` は
**「N 人へ流しました。」**と返すか、断るようにした。

```
chat_send → この PC の画面には出ましたが、会議には誰も居ないので誰にも届いていません
```

**試験は全部緑だった。実物を動かさなければ出なかった。**

#### 画面で確かめたこと（2026-09-07 18:11・Mac mini）

```
机を開きました: ~/Library/Application Support/warifu/desk.sock   （srw------- = 0600）
```

`warifu mcp` から `chat_send` を叩き、**画面のチャット欄に出るのを目で見た。**

```
18:11   この PC の AI   机から着きました
```

**まだ見ていないこと** —— 相手の PC に届く所（`send_text` と同じ `outbound` を通るが、
机ごしでは実測していない）。3 台での AI 同士の会話。

#### 会議に人が居なくても、同じ席の AI に話しかけられるようにした

オーナーが実際に「こんにちは」と打って、**送れなかった**（2026-09-07）。

**AI は画面に書けるのに、人は AI に返せない。**片側だけの経路になっていた。
会議に人が 1 人も居ないと `送る` を押せない形にしてあったためである（D49 の適用しすぎ）。

> **まず会議ありきではなく、チャットさえつながって**、
> できればそれが GUI だとぐんと作業効率が上がります（オーナー・2026-09-06）
> **会議ありきのチャットじゃないんです。**

直した形 ——

- **机に誰か着いていれば送れる。**会議が無くても、相手が 0 人でも
- 画面に「**この PC の AI が居ます。会議に人が居なくても話しかけられます。**」と出す
- 打ち込み欄の案内も「**この PC の AI に話しかける**」に変える
- 記録の 1 行に `机 N 人` を足した（**居たのか居なかったのかが後から読めない**ため）

**「相手が居ない」と「話し相手が 1 人も居ない」は違う。**
判定は `届く先がある`（`$lib/meeting/stage`）に出して試験を 3 本置いた。

試験 **435 本 green ＋ 画面側 127 本 green**（新規 43 本）。

### **Windows で画面が建つようになった**

2026-09-03 から「**Windows では一度も建てたことがない**」状態が続いていた。
**「上流の問題でこちらでは直せない」と書いたが、誤りだった。**

`wmi 0.18.4` は `windows` と `windows-core` の**両方を `">=0.59, <0.63"`** で要求している。
緩い範囲なのに、画面側の `Cargo.lock` に
**`windows 0.61.3` ＋ `windows-core 0.62.2` という食い違った組が固定されていた。**
**ロックの慣性である。**`cargo update -p wmi` で解決し直したら揃った（**差分 3 行**）。

出るもの ——

```
warifu_0.1.0_x64_en-US.msi     8.2 MB   画面付き
warifu_0.1.0_x64-setup.exe     5.5 MB   同じもの（形が違うだけ）
warifu.exe                    14.8 MB   CLI
```

**署名していない。**初回は「WindowsによってPCが保護されました」が出る ——
**「詳細情報」→「実行」**で進む（`docs/install.md`）。

**押せるのに効かないボタンを 2 つ直した**（**D49**）。どちらも**最初に触った人が必ず踏む**形で、
実際にオーナーから「**チャット送るボタンきかないよ**」と報告された。

| | いつ押せなかったか |
|---|---|
| チャットの「送る」 | **相手が居ないとき。**押すと「まだ誰も居ません」と返るだけで、何をすればよいか言っていなかった |
| 会議中のマイク／カメラ入切 | **その機器が無い機械。**押すとアイコンだけ切り替わり、**動いたように見えて動いていなかった** |

**押せないものは押せない形にし、理由をその場に出す。**打ち込んだものは消さない。

**CLI の主催が複数人を受けるようになった**（**D50**）。
それまでは 1 人しか受けられず、**エージェントだけで三者会話ができなかった**（画面が必ず要った）。

```
$ warifu host --keys 2
warifu: 会議キーを 2 本出します。1 本につき 1 人です
```

**Windows でも主催になれる。**（この時点では Windows は CLI だけだったが、
**同じ日に画面も建つようになった** —— 下記。）

実測（画面を使わず、エージェント 3 名だけ・**1 台の中**）——

| 向き | 通数 | 中央 | 最大 |
|---|---|---|---|
| 主催 A → B と C | 20 | **3.3 ms** | 4.8 ms |
| B → A と C（C へは主催が中継） | 20 | **3.3 ms** | 4.6 ms |
| C → A と B（B へは主催が中継） | 20 | **3.0 ms** | 7.9 ms |

**60 通すべて届いた。**中継を挟んでも差が出ていない。
**ただし 1 台の中の数字で、経路の下限でしかない。**別の機械どうしは測っていない。

そのほか ——

- 受信箱（`docs/inbox.md`）に**規則のひな型**を足した。**規則が無いと初日は全部 `unknown`** になり、
  この層の目的（AI を呼ばずに済んだ通数）が測れない
- **訳文レビューのシート**を出した（`docs/i18n-review.tsv`・73 鍵 × 4 言語）
- 端末に `**` がそのまま出ていたのを直した（**端末は Markdown を解釈しない**）

---

## v0.1.0-alpha.2 — 2026-09-06（**最初に配ったもの**）

`v0.1.0-alpha.1` は `release` が落ちて配布物ができなかった
（**署名の材料が無いのに署名しようとした**）。**なぜ alpha.2 なのかを辿れるようにタグは残してある。**

**最初のアルファ。うちうちの試験導入向け。**署名していないので、受け取る側に
1 回だけ操作が要る（`docs/install.md`）。**Apple Silicon の Mac だけ確かめている。**

### できること

- **2 人の会議**（映像・音声・文字）。割符で相手を確定してから繋ぐ
- **3 人以上の会議**（**D47** / **D48**）。会議キーを人数ぶん出す。文字は主催が全員へ配る
- **予定に紐づく会議キー**（**D43**）。「いつから・いつまで」が署名の中に入る
- **切れても戻れる**（**D44**）。回線が一瞬切れた相手は、同じ会議キーで戻ってこられる
- **閉じても同じ人でいられる**（**D42**）。身元がこの端末に続く
- **相手を覚える**（**D46**）。呼び名を付けると、名簿・映像・チャットにその名前で出る
- **画面なしで使える口**（CLI）。エージェントどうしが人手なしでやり取りできる
- **4 言語**（en / ja / zh / ko）。**訳文は AI の下書きで、人のレビュー前**

### 直したもの（2026-09-04 〜 09-06）

| | |
|---|---|
| `warifu host` が落ちても終わらなかった | 標準入力の読み取りを待って畳めていなかった。**55 分残っていた実績**。33 秒で終わるようになった |
| 「帰った」と「落ちた」が同じ文言だった | 人はこの 2 つで次の手が変わる。分けた（`warifu_net::Error::Closed`） |
| 画面の名簿だけ「退室しました」のままだった | 帯は「経路が切れました」なのに食い違っていた |
| CLI が SDP を 10 進のバイト列で吐いていた | **1 行 4232 バイト → 79 文字。**会議の下ごしらえが端末とログに残っていた |
| **チャットと名簿が窓の外にあった**（**D45**） | 右パネルが窓の下端から **442px** はみ出していた。画面に 3 つの状態を持たせて解消 |
| 会議キーを出した後、作り直せなかった | D45 で入れてしまった不具合。「もう 1 本出す」を置いた |
| カメラもマイクも無い機械で ☑ のままだった | 同じ画面が「無いので受け取るだけで入ります」と言っていた |
| 署名した本人と、繋いできた相手を突き合わせていなかった | 写し取った片割れを別の経路で出せてしまう（**D44** と併せて塞いだ） |

### 分かっている限界

- **署名も公証もしていない**（`docs/install.md` の手順が要る）
- **画面は Apple Silicon の Mac と Windows（x64）。**Intel Mac は建て直しが要る
- **Linux は確かめていない**
- **自動更新は無い**（設計だけ・**D36**）。新しい版は入れ直す
- **チャットは残らない。**閉じると消える（保存には **D2** の決着が要る）
- **中継を使っていない。**対称 NAT・厳しい社内網では繋がらない（**D13**）
- **3 人以上の文字は星形**（主催が配る・**D48**）。フルメッシュは M6
- **映像の 3 人以上は確かめていない**（カメラ 2 台以上が要る）
- **会議キーは 1 本につき 1 人。**公開して使い回せる鍵（会場鍵）は無い（`.claude/issues/009`）
- **帯域と CPU の実測をしていない。**定員 12 は文献値のまま（**D27**）
