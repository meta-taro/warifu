# DESIGN.md — warifu

> **状態**: **確定（2026-09-02・オーナー承認済）**/ **出典**: `md-business/apps/desktop/DESIGN.md`（451 行）と
> `dbboard/DESIGN.md`（210 行）から**実際の値を引き写した**もの。
> **warifu が独自に決めたのは §4 の 3 つだけ**である。
>
> baseline §11 は「AI が先回りして見た目を決めない」と書いている。ここで AI が決めていないのは
> **方向性**であり、それは「dbboard と md-business を参考に」というオーナー指示（2026-09-02）が与えた。
> AI がやったのは**既存 2 つから値を取り、warifu の語彙へ翻訳すること**である。
>
> **止めるべき条件は §14 に書いた。**そこに当たるなら、確定後でもこの文書ごと差し戻してよい。
>
> **2026-09-02**: 実物の色で並べたレビュー面を人が見たうえで「これで十分」と承認した。
> 以後、この文書が warifu の見た目の**正本**であり、実装はここを唯一の出典とする（§13）。

---

## 1. warifu が画面を持つ範囲

`PRD.md` §0-c のとおり、warifu は Identity / Trust / Permission / Agent の層であり、
**画面は「人が判断しないと進まない操作」にしか作らない。**

| 画面 | いつ要るか |
|---|---|
| **連絡帳** | **いつも。入口である**（**D57**）。相手を選んで、会議・チャット・メールを決める |
| **予定** | 会議の時刻を決めるとき（**いまは枠だけ**） |
| **割符の受け渡し** | 招待を出す・受ける（QR / 文字列） |
| **端末の承認** | 2 台目以降を同じ Profile に足す |
| **失効** | 鍵を失った端末を降ろす（**取り消せない**・D12） |
| **会議** | M5 以降。映像・音声・画面共有（`issues/005`） |
| **受信箱** | Phase 3-b。**読むだけ**（`warifu-read` / `warifu-mcp`） |
| **更新の通知** | §10。待機中のチップと、適用の確認 |

**それ以外に画面を作らない。**特に §11 に挙げたものは、作らないことが仕様である。

---

## 2. デザイン原則

md-business の §1（Linear 風の核）をそのまま継ぐ。

1. **静けさ優先** — 装飾は最小。色・影・境界線は必要なときだけ。情報が主役
2. **余白で階層を作る** — 罫線より余白
3. **繊細なアクセント** — indigo は「操作可能な一点」にだけ使う
4. **速いフィードバック** — 120–160ms の ease-out
5. **キーボードファースト** — マウスなしで主要操作が完結する

warifu 固有に足すのは 3 つ。

6. **状態を色だけで言わない**（dbboard の "Colour is never the only mark" を継ぐ）。
   **経路が直接か中継か、鍵が生きているか失効か**は、色 + ラベル + 形の**三重**で示す。
   ここを色だけにすると、色覚多様性でも greyscale のスクリーンショットでも読めない
7. **繋がっていないことを、繋がっているように見せない。**
   「たぶん届いている」を表す表現を持たない。**分からないときは分からないと出す**
8. **人の注意は資源である**（**D31**）。知らない相手が通知を出せる導線を作らない

---

## 3. カラートークン（**アイコンから採る**・**D73**・2026-09-08）

**色はアイコンから採る。**オーナー指示（2026-09-08）——
「**まず全体の色味をアイコンで決めてきた色調にしたほうがいいです。
ライト・ダークテーマを切り替えられつつ、ボタンなどの配色について**」。

| 出どころ | 値 | どこで使うか |
|---|---|---|
| **地**（カフェオレ） | `#cdb295` | ライトの地の色味。**ダークでは操作の色** |
| **板**（木） | `#7b5a3e` | ライトの操作の色（`--accent`） |
| **木目**（濃い木） | `#6b4c33` | ホバー |

**2026-09-08 まで md-business の値（藍 `#5b5bd6`）を引き写していた**（**D33**）。
理由は「M7 で画面共有に md-business の文書を映すので、色味が食い違うと
どちらの不具合か分からなくなる」だった。**その代償として、アプリの顔と画面の色が
別物になっていた** —— 帯のブランドだけがアクセント色の四角で、
**アイコンだと思って見た人が「むかしのアイコンですか」と尋ねた。**

**M7 の理由は消えていない。**画面共有で 2 つが並ぶことは変わらない。
**それでも、自分のアプリの色を持たないほうが高くつく**と判断した（D73）。

### 3.1 アクセント（**操作できる一点**）

| トークン | ライト | ダーク | 用途 |
|---|---|---|---|
| `--accent` | `#7b5a3e` | `#cdb295` | 主要操作・選択・フォーカスリング |
| `--accent-hover` | `#6b4c33` | `#dbc4aa` | ホバー |
| `--accent-active` | `#5a3f2b` | `#e6d4bf` | 押下 |
| `--accent-subtle` | `#f3e9dd` | `#2e2419` | 選択行の地・淡いバッジ |
| `--accent-border` | `#e0cdb6` | `#453626` | 淡い枠 |

**暗い所では木が沈む。**ダークでは**地と板の役割を入れ替える**（板 → カフェオレ）。
色そのものは 2 色のままで、**明暗の役割だけを入れ替えている。**
入れ替えたので、`--text-on-accent` はダークで**濃い文字**になる（白のままだと読めない）。

### 3.2 ニュートラル（**カフェオレへ寄せた温かい灰**）

| トークン | ライト | ダーク |
|---|---|---|
| `--bg-app` | `#fffdfa` | `#14100c` |
| `--bg-subtle` | `#faf6f0` | `#1a1511` |
| `--bg-sunken` | `#f2ebe1` | `#211a14` |
| `--bg-elevated` | `#fffdfa` | `#231c16` |
| `--bg-hover` | `#f5eee4` | `#2b231b` |
| `--border` | `#e7ddd0` | `#31281f` |
| `--border-strong` | `#d5c6b3` | `#42362b` |
| `--text-primary` | `#241d16` | `#f6f1ea` |
| `--text-secondary` | `#6b5c4c` | `#b3a493` |
| `--text-tertiary` | `#9b8c7b` | `#7d7062` |
| `--text-on-accent` | `#fffdfa` | `#241d16` |

**灰を無彩色にしない。**無彩色の灰の上に木の色を置くと、**木だけが浮いて見える。**

### 3.3 セマンティック（地 / 文字）

**意味の色は、意味のまま残す。**緑は通った、赤は危ない ——
ここを色調に合わせて動かすと、**意味が読めなくなる。**地だけを温かい側へ寄せる。

| 意味 | ライト | ダーク |
|---|---|---|
| `--success` | `#e8f3e4` / `#2f6f3a` | `#14251a` / `#86c98d` |
| `--warning` | `#fbf0dc` / `#8a6412` | `#2a2010` / `#e5bd6f` |
| `--neutral` | `#f2ebe1` / `#6b5c4c` | `#241d16` / `#b3a493` |
| `--danger` | `#f9e9e4` / `#a83a2c` | `#2a1512` / `#e8998a` |
| `--info` | `#eaf0f2` / `#3a6274` | `#14212a` / `#8fb4c4` |

**ライトを主軸に設計し、ダークは同じトークン名で値だけ差し替える**（md-business と同じ）。
`color-scheme` をテーマと同期させ、WebView のスクロールバーまで追従させる。

---

## 4. warifu 固有のセマンティクス（**ここだけが新規**）

既存 2 つに無く、warifu が持たなければならない軸。**セマンティックとは別の第 3 軸**で、
呼び出し側がアクセントから借りてはいけない（dbboard の identity colours と同じ扱い）。

### 4.1 経路（`issues/005` 満たすこと 2）

| 状態 | 色 | ラベル | 形 |
|---|---|---|---|
| **直接** | `--success` | `直接` | ● 塗り |
| **中継経由** | `--warning` | `中継` | ◐ 半分 |
| **不明 / 測定中** | `--neutral` | `不明` | ○ 抜き |

**「中継」を隠さない。**中継を通ったという事実は、通信の性質が変わったという意味であり、
**黙って良い状態として扱わない**（原則 7）。ただし**警告としても出さない** — 中継は異常ではない。

### 4.2 鍵と割符

| 状態 | 色 | ラベル |
|---|---|---|
| **有効** | `--success` | `有効` |
| **失効済** | `--danger` | `失効` |
| **期限切れ**（割符） | `--neutral` | `期限切れ` |
| **未使用**（割符） | `--accent` | `未使用` |

**失効は取り消せない**（D12）。したがって失効の操作には**元に戻す導線を置かない。**
「取り消せます」と読める表現（`元に戻す` / `やり直す`）を UI から排除する。
戻せると誤解させることが、鍵を盗った側に降ろす口を与える。

### 4.3 会議の定員（D27）

定員は会議ごと（既定 **12** / 外枠 **16**）。名簿には `7 / 12` の形で**現在数と定員の両方**を出す。
**定員を UI で 16 より大きく入力できないようにする**（受け取る側でも数える・D15）。

---

## 4-A. ロゴ（**2026-09-02 オーナー指示「割符のアイコン」**）

**割符**（わりふ）— 二つに割った札。**片割れが合うことで、相手が確かにその相手だと分かる。**
アイコンはその一点だけを描く。**鍵も錠も盾も出さない**（それは割符の意味ではない）。

| | |
|---|---|
| **地** | 角丸の四角。**カフェオレ**（`#cdb295`・ミルク多め）。**飲み物の色**であって、木の色ではない |
| **印** | **木の板**（`#7b5a3e`）。**縦長** — 割符は棒状の札を縦に割る |
| **割れ目** | 縦のギザギザ。**左右がぴったり噛み合う**（片方をずらして描いていない） |
| **木目** | ごく薄く縦に（`#6b4c33`）。**128px 未満では描かない** |

### 地と板で役割を分ける

**両方を木の色にすると、背景まで木に見えて「板の上に板」になる**（一度そうなった）。
地は飲み物、板は木。**明暗差がそのまま割れ目の読みやすさ**なので、
片方を動かしたら `GAP` も見直すこと。

### **アクセント色を使っていない**

`--accent`（`#5b5bd6`）は UI の「操作できる一点」の色である。
アイコンは**物**を描いているので、そこから外した
（オーナー指示 2026-09-03「モカ、カフェオレみたい。木の板をイメージ」）。

これは §3 の一貫性を崩す判断に見えるが、**崩していない** —
§3 が縛るのは画面の中のトークンであって、**アプリの顔ではない**。
dbboard はロゴにアクセントを使い、warifu は使わない。**どちらも意識して選んでいる。**

### 木目は滑らかに振る

行ごとに位置を飛ばすと、**木目ではなく破線（ミシン目）に見える**（一度そうなった）。
`sin` で連続に振ること。

### 32px で読めることが要件

歯は **2 つ**、割れ目は太い。細かいギザギザにすると、
**小さい時に潰れて「ただの白い四角」か「ノイズ」に戻る**（3 歯・細い割れ目で試して、実際にそうなった）。

### 家族の決まりとの差（**承知のうえで残す**）

`.claude/templates/design-system/product-icon-and-visual-ja.md` §3 は、
デスクトップ製品のアイコンを**家族として揃える**数値を持っている。**warifu は 2 つ外れている。**

| | 家族の決まり | warifu | |
|---|---|---|---|
| 外周の余白 | 9% | **0%** | 縁まで塗っている |
| 角丸 | 四角の 22% | **18.6%** | |
| 地 | ベタ 1 色 | ✅ | |
| 印の色 | 白（**地が淡いときは濃色**） | ✅ 濃色 | 地がカフェオレなので例外に当たる |

**揃えない。**雛形の §1 が「**これを理由に既存を直す作業を起こさない**」と定めており、
雛形自身が warifu の実測値を記録している。**この形は人が見て承認したもの**（2026-09-03）で、
規則に合わせるためだけに動かさない。

次にアイコンを作り直すときは、**先に §3 を読んでから**始める。

### 出処と権利

**手で書き起こした**（`scripts/make-icon.py`）。第三者の素材・トレース・ライセンス品を
**一切使っていない**ので、外部の権利関係は無い。プロジェクトのライセンス（Apache-2.0 OR MIT）で配れる。

依存を足さないため、PNG は `zlib` と `struct` だけで書いている。
`.icns` は macOS の `iconutil`、`.ico` は PNG を並べた最小の容器を自前で組んでいる。

```bash
python3 scripts/make-icon.py   # 作り直す
```

**色や形を変えるときは、このスクリプトと本節を同じ commit で直す**（baseline §10）。

---

## 5. タイポグラフィ

md-business §3 をそのまま。

- **UI**: `Inter` → 日本語 `Noto Sans JP` → `system-ui, -apple-system, "Hiragino Sans", sans-serif`
- **等幅**: `"JetBrains Mono", ui-monospace, "SFMono-Regular", monospace`
  — **割符・宛先・公開鍵・会議 id はすべて等幅**（`WARIFU1-` + base32 は 1 文字の違いが意味を変える）

| トークン | size / line / weight | 用途 |
|---|---|---|
| `--text-2xs` | 11 / 16 / 500 | バッジ・キャプション |
| `--text-xs` | 12 / 18 / 400 | メタ情報 |
| `--text-sm` | 13 / 20 / 400 | **UI 既定** |
| `--text-base` | 14 / 22 / 400 | 本文 |
| `--text-md` | 16 / 24 / 500 | パネル見出し |
| `--text-lg` | 20 / 28 / 600 | 画面タイトル |
| `--text-xl` | 24 / 32 / 600 | 空状態の見出し |

見出し（`--text-lg` 以上）は `letter-spacing: -0.01em`。等幅は `0`。
帯域・人数・経過時間は `font-variant-numeric: tabular-nums`（桁が動くと読み違える）。

---

## 6. スペーシング・角丸・影・モーション

md-business §4 をそのまま。

- **4px グリッド**: `--space-1: 4` / `-2: 8` / `-3: 12` / `-4: 16` / `-5: 24` / `-6: 32` / `-8: 48` / `-10: 64`
- **角丸**: `--radius-sm: 6` / `--radius-md: 8`（既定）/ `--radius-lg: 12` / `--radius-full: 999`
- **影**: ライトは低アルファ、ダークは弱めて境界で起伏を作る
- **モーション**: `--ease: cubic-bezier(0.2, 0, 0, 1)` / `--dur-fast: 120ms` / `--dur-base: 160ms` / `--dur-slow: 240ms`
- `prefers-reduced-motion` で全アニメーションを `1ms` に落とす

**映像の枠には動きを付けない。**参加者の出入りでレイアウトが跳ねると、会話が途切れる。

---

## 7. コンポーネント（初期範囲）

| | 中身 |
|---|---|
| **割符カード** | QR + 等幅の文字列 + 期限。**QR と文字列を必ず両方出す**（紙・口頭でも成立する・M1） |
| **経路インジケータ** | §4.1 の 3 状態。参加者ごとに 1 つ |
| **名簿** | 参加者一覧。`現在数 / 定員`。主催者に印 |
| **端末一覧** | Profile に紐づく端末と、その状態（§4.2） |
| **失効の確認** | **二段階**。1 段目で対象を読み上げ、2 段目で「取り消せません」を明示 |
| **受信箱** | 読むだけ。**返信・削除・既読を付ける口を出さない**（`BODY.PEEK[]`・D23） |

各コンポーネントは、作った時点でこの節に仕様を足す（dbboard の運用に合わせる）。

### 7.1 面と、画面の状態（**D45 ＋ D57**・2026-09-07）

**画面は「面」と「状態」の 2 つの軸を持つ。混ぜない。**
**入口は連絡帳である。**起動して最初に出るのは会議ではない ——
割符はビデオ会議が中心のアプリではない（オーナー・2026-09-06）。

#### 面（`$lib/shell/panes.ts`）

| 面 | 左 | 右 |
|---|---|---|
| **連絡帳**（既定） | この PC（自分 ＋ この PC の AI）／いま会議に居る人／覚えている相手 | 選んだ相手に出す口（チャットする・会議に呼ぶ・**メール＝押せない**）＋ 会話 |
| **会議** | 知らせ ＋ 映像 ＋（会議中）マイクとカメラの入切 | 名簿 ＋ 会話 ＋（会議前）したく・会議をはじめる・入室する ＋ **預かり所（任意）** ＋（鍵があれば）会議キー（畳） |
| **予定** | **まだ動かないことを、理由つきで出す**（§2 原則 7） | |

#### 状態（`$lib/meeting/stage.ts`・**D45 のまま**）

| 状態 | 帯の中央 | 会議の面の右の列 |
|---|---|---|
| **会議前** | （空） | したく / 会議をはじめる / 入室する ＋ 名簿 ＋ 会話 |
| **待っている** | 相手を待っています | 会議キー（畳）＋ 名簿 ＋ 会話 |
| **会議中** | 会議中 | 会議キー（畳）＋ 名簿 ＋ 会話 |

#### 面をまたぐ決めごと

- **会議の面は畳まない。隠すだけ。**外すと**相手の音が消える**（音は `<video>` から出ている）。
  `[hidden]` の打ち消しは**スタイルの最後に置く** —— 同じ強さの `.pane.meeting` に負ける
- **面は人の操作でだけ移る。**相手が入ってきたことで面を移さない（§2 原則 8 / **D31**）
- **会話は 1 本。**面をまたいで同じ会話を出す。**相手ごとの会話があるように見せない**（原則 7）——
  **ただし預かり所ごしの 1 対 1 は例外**（**D71** / **D72**）。封をした相手しか開けられないので、
  そこでは「この会話は 1 本です」を**出さない**（出すと、事実と食い違う）
- **留守中に届いた分は、そう分かる形で出す**（**D72**）。印（`留守中`）を付け、
  時刻は**出した側の時計**で出す。**いま届いたように見せない** ——
  3 日前の言葉が、たったいま言われたように読める
- **`n / 定員` は名簿にだけ**（§4.3 / D27）。連絡帳の「いま会議に居る人」には出さない

#### 会議の面の中の決めごと（**D45 のまま。変えていない**）

- **知らせは映像の上。**下に置くと目に入らない（会議中の目線は帯の直下か映像の中）
- **窓ごとスクロールさせない。**動いてよいのは列の中だけ。映像を流さない
- **会議キーの全文は畳む。**消しはしない（§7 の割符カード）。畳んだままでも「コピーする」は押せる
- **映像は縦中央。**幅で高さが決まる（16:9）ので伸ばせない。余りは上下へ振り分ける

---

## 8. ウィンドウクローム（**フレームレス自作タイトルバー**）

**OS ネイティブのタイトルバーを使わない**（`tauri.conf.json` の `decorations: false`）。
上端の帯そのものをタイトルバーとして働かせる。VS Code / Linear / md-business と同型
（**オーナー指示 2026-09-02**・md-business `DESIGN.md` §6.5 と同じ結論）。

**理由**は見た目の統一である。ネイティブ窓枠はテーマに追従しないので、
**ダーク時に白いタイトルバーだけが浮く。**窓枠ごと自作 HTML にすれば、
ライト / ダークが**窓全体で**揃う。

| | 決めること |
|---|---|
| **ドラッグ** | 帯の地に `data-tauri-drag-region`。左右の中身は `pointer-events: none` で地へ貫通させ、**どこを掴んでも窓が動く**。ボタン群だけ貫通させない |
| **窓の操作** | 右上角にフル高で密着（Fitts の法則）。最小化 `─` / 最大化 `▢`⇄復元 `❐` / 閉じる `✕`（ホバーで `--danger-fg`） |
| **高さ** | `--topbar-h: 44px` |
| **影と角丸** | `shadow: true` を維持し、Win11 では DWM 経由のリサイズ枠・角丸・影を残す |

**Windows で失うもの**を先に書いておく。最大化ボタンをホバーしたときの
**Win11 のスナップ・フライアウト**は出なくなる（ネイティブのキャプション判定が要る）。
端のドラッグと `Win + 矢印` のスナップは効く。**後続の磨きとして扱い、M5 の完了条件には入れない。**

**テーマの切り替えは、メニューバーに置く**（**表示 → OS に合わせる / ライト / ダーク**）。
帯には置かない —— 帯に載せるのは下の 3 つだけである。

**帯に載せるのは 3 つだけ** — 左にブランド、中央に今の状態（会議名 / 受信箱）、右に窓の操作。

**左のブランドは、アプリの顔と同じ物を描く**（§4-A の割符・地はカフェオレ、板は木）。
**`--accent` を使わない。**アクセントは「操作できる一点」の色であって、アプリの顔ではない ——
2026-09-08 まで、ここだけアクセント色の四角（`brand-dot`）が置かれていた。
**アイコンだと思って見た人が、古いアイコンかと尋ねた**（オーナー・2026-09-08）。
`issues/005` の経路インジケータ（§4.1）は**帯に置かない**。参加者ごとに出るものなので、名簿の側に付く。

---

## 9. 多言語（**en / ja / zh / ko**）

md-business と**同じ 4 言語・同じ表記・同じ並び**にする（`src/lib/i18n/locales.ts`）。

| 値 | ラベル |
|---|---|
| `en` | English |
| `ja` | 日本語 |
| `zh` | 中文 |
| `ko` | 한국어 |

- **並びは `en → ja → zh → ko` で固定。**既定は OS の言語、取れなければ `en`
- **辞書は DOM 非依存の純データ層に置く**（`messages.ts`）。反応状態と永続化は別の層
- 切替は帯の右側。**最狭段では地球儀アイコンだけ**にする（`<select>` は `…` で省略できないため、
  透明のまま当たり判定として重ねる）

### 翻訳しないもの

**割符・宛先・公開鍵・会議 id は翻訳しない。**`WARIFU1-` + base32 は 1 通りの表記しか許さない（M1）。
言語ごとに見た目が変わると、口頭で伝える経路が壊れる。

### 誤訳が致命傷になる文言

**失効（§4.2）と戸口（§11）の文言は、意味が反転すると事故になる。**

- 「**取り消せません**」が「後で戻せます」と訳されたら、人は軽く押す
- 「**断りました**」が「保留しています」と訳されたら、来ていない相手を待つ

この 2 つは**翻訳者への注記を辞書に残す**。機械翻訳をそのまま採らない。
**AI が訳文の可否を決めない**（baseline §19 / §27 — 実物を見た人が判断する）。

---

## 10. 自動アップデート

**Tauri の updater を使う**（`@tauri-apps/plugin-updater` v2）。
配布は **GitHub Releases**、検証は **minisign 署名**。md-business と同じ構成にする。

| | 決めること |
|---|---|
| **取得** | 自動。起動時と、以後 1 日 1 回 |
| **適用** | **人が押す。**待機中は帯に控えめなチップ（dbboard の status bar と同じ考え） |
| **署名** | **検証を外す設定を置かない。**未署名の更新は適用しない |
| **Windows** | `installMode: passive`（進捗は出すが、操作は求めない） |
| **配布元** | `https://github.com/<owner>/warifu/releases/latest/download/latest.json` |

### 鍵は人が作る（**baseline §14**）

**minisign の秘密鍵を AI が作らない・置かない・貼らない。**
公開鍵（`pubkey`）は `tauri.conf.json` に入るので公開して構わないが、**生成は人が行う。**
CI で署名する場合、秘密鍵は GitHub Secrets へ**人が投入する。**

### なぜ warifu では特に慎重に扱うか

**自動更新は「外から入ってくるコードを、利用者の同意なしに実行する経路」である。**

warifu は E2EE の道具で、鍵と失効を扱う。ここが破られると、
**暗号の強さとは無関係に全部が抜ける。**だから

- **署名検証を無効化する経路を作らない**（設定にもコマンドラインにも置かない）
- **更新の適用前に、何のバージョンへ上がるかを人へ見せる**
- **更新で失効の意味を変えない。**失効は取り消せない（D12）——
  これは更新をまたいでも変わらない不変条件として扱う

---

## 10-A. UX の既定（**言われる前にこうする**）

2026-09-10、オーナーに 1 つずつ言わせてしまった。
「**いちいち指定させないで・つくりなおしがおおいのもきみがおそいりゆう**」
「**いちいち指摘しなくても君は本来できるはずなんだけども**」——
そのとおりである。**以後、指示を待たずにこの形で作る。**

| 場面 | 既定 |
|---|---|
| **状態の印** | **知っていることは印で出す**（顔の右下にバッヂ。緑＝つながっている、グレー＝切れている）。**知らないことには印を出さない** |
| **説明の口** | 説明が要る区画には、**その枠の右上の角**に淡い灰色の丸「?」。**見出しの文字に付けない** |
| **説明・確認の出し方** | **まんなかに、透過の黒＋ぼかしの幕**で出す。**畳んで開く形（アコーディオン）にしない**。一覧を押し下げない |
| **見るだけの幕** | **閉じる口を置かない。**どこを押しても閉じる（Esc でも閉じる） |
| **消える操作** | **押したら、まんなかの幕で確かめる。**消えるものを文に書く（「差し替えた画像は消えます」） |
| **押せる絵** | **顔は押したら大きくなる。**人は押せば大きくなると思う（`cursor: zoom-in`・ホバーで少し浮く） |
| **できることの並び** | **札にする。**札には**できる／条件つき／まだできない**と、その 1 行を必ず付ける（D88） |
| **生の id** | **人に見せない。**名前が無ければ「ルーム 1（3 人）」のように数えて呼ぶ。id は詳細の欄だけ |
| **言葉** | **部屋・机・席・居る は使わない**（**D89**）。ルーム／この PC のエージェント／入っている・つながっている |
| **色** | `tokens.css` のトークンだけ。**色だけで意味を言わない**（必ず文字を添える） |

**この表に無いものを新しく作るときは、この表に足してから作る。**

## 10-B. 打ち込む所（**唯一の出典は `lib/styles/forms.css`**）

オーナー指示（2026-09-10）——「**インプットのデザインをモダンにできないですかね**」

それまで**部品ごとに書いていた**ので、同じ画面の中で高さも角も違っていた。

| | |
|---|---|
| 地 | `--bg-sunken`（**沈んだ地**。押せるもの＝木の色と見た目で分ける） |
| 高さ | 34px（行の中に置くものだけ 28px まで詰める） |
| 角 | `--radius-md` |
| 触れたとき | 縁を `--border-strong` へ |
| 打っているとき | 縁が操作の色、地が `--bg-app`、外に `--accent-subtle` の薄い輪 |
| 触れない所 | 文字は `--text-tertiary`、地は `--bg-subtle`、`not-allowed` |
| 選ぶ所 | 三角は自前で描く（機械ごとの見た目に振られない） |
| ☑ | `accent-color` で操作の色。**自前で描かない**（読み上げが拾えなくなる） |
| 数・日・時刻 | 等幅の数字（桁を読み違えない） |

**部品側で上書きしてよいのは、幅と行数だけ。**
会議キーだけは等幅にする（1 文字の違いが意味を変える・§5）。

**幕（まんなかに出す窓）は `lib/styles/overlay.css`。**
Svelte の `<style>` はその部品の中だけに当たるので、
**共通の見た目を部品に書くと、別の部品では素のまま出る**（2026-09-10 に踏んだ）。

## 11. **作らないもの**（決定が UI に効く所）

ここは「まだ作っていない」ではなく、**作らないと決めた**ものである。

| 作らないもの | 根拠 |
|---|---|
| **「知らない相手が来ました。通しますか」** | **D31**。通知を出せること自体が資源で、開ければ spam の入口になる。答えは `Open` と `Refuse` だけ |
| **AI からの承認ダイアログ** | **D25**。MCP には承認の口を出さない。人が押す所を AI が叩ける導線を作らない |
| **信頼度スコアの表示** | **D24**。信頼を判定に使わない。数字を出すと、それで判断され始める |
| **失効の取り消し** | **D12**。§4.2 のとおり |
| **秘密情報の生成ボタン** | baseline §14。API キー・接続文字列は**人が入れる**。「生成」を UI に置かない |
| **「5 人以上にする」設定** | **D27 / D7**。外枠 16 を超えるには中継が要り、法的整理が済んでいない |
| **署名検証を切る設定** | §10。更新経路は E2EE の外側にある。ここを開けると暗号の強さと無関係に抜ける |
| **ネイティブのタイトルバー** | §8。テーマに追従せず、ダーク時に白い帯だけが浮く |
| **在席の表示**（相手が起動しているか） | §2 原則 7。**分からない。**分からないものを分かるように見せない（`issues/010` 段 4） |
| **相手ごとの会話（DM）に見える形** | 会話は 1 本。個別に届くように見せると、**見られたくないものを書く** |
| **メールの「送る」が押せる形** | **D49。**送る経路が 1 本も無い。押せる形にして断らない |
| **「呼ばれています。出ますか」** | **D31。**連絡帳から呼ぶのは自分の側だけである |

---

## 12. アクセシビリティ

- コントラスト **WCAG AA**（本文 4.5:1 / 大文字 3:1）を**両テーマで**満たす
- フォーカスリング常時可視（`--accent-subtle` 3px）。キーボード操作に完全対応
- `prefers-reduced-motion` を尊重
- **状態は色 + ラベル + 形の三重符号**（§2 原則 6）。色だけの行は不具合であって、省略形ではない
- OS のフォント倍率に追従する

---

## 13. 実装方針

- **トークンの唯一の出典は CSS custom properties**。実装時に
  `apps/desktop/src/lib/styles/tokens.css` を置き、**この文書と同じ commit で直す**（baseline §10）
- コンポーネントに**色をハードコードしない**。必ずトークンを参照する
- ライトを `:root`、ダークは `:root[data-theme='dark']` で**値だけ**上書き
  （`@media` との二重定義を持たない — ダーク値の管理が二重化して必ずずれる）
- **フォントは同梱する。**繋がらない環境でも同じに見える必要がある（baseline §3）

---

## 14. これを止めるべき条件

- **アクセントを md-business と分けたい**とき。§3 は「並べて映すから揃える」を根拠にしているが、
  **別プロダクトとして立てたい**なら根拠ごと変わる。その場合 `--accent` 系 6 個だけ差し替える
- **§4 の 3 軸が実物と合わない**とき。特に §4.1 は、WebRTC の統計から
  「直接か中継か」を実際に取れるかを M5 で確かめるまで**机上である**
- **fontsource 同梱がライセンス上まずい**と分かったとき（Inter / JetBrains Mono / Noto Sans JP はいずれも OFL）
- **自作タイトルバーが Windows で問題を起こす**とき（§8）。Win11 のスナップ・フライアウトが
  実際に要ると分かったら、**ネイティブ枠へ戻すのではなくキャプション判定を足す**方を先に検討する
- **4 言語が多すぎる**とき（§9）。訳文の質を保てないなら、**言語を減らすほうが正しい**。
  読めない訳を出すより、英語だけのほうが害が小さい
- **人がこの見た目を望まない**とき。これが一番強い理由で、他の根拠に優先する
