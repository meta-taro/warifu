{
  "name": "warifu",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint"
  }
}
