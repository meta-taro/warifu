# 001. 3 台のルーム（検証シートの読み方）

シート: **`001-3台のルーム.tsv`**

> **判定と実測値は、実物を見た人が書きます**（`.claude/rules/product-baseline.md` §19）。
> **AI が代筆しません。**空のままが「まだ見ていない」です。`—` や `N/A` で埋めないでください。

## なぜ書いたか

**2026-09-15、オーナー指摘** ——

> **テストパターンも不明なんだよね。**

**そのとおりだった。**`docs/test-specs/` には README しか無く、
**9 日間、測るものをその場で決めていた**（2026-09-06 に「3 台目は Win」と聞いてから今日まで）。
**同じ所で止まっていることが、記録からも読めなかった。**

## 使い方

1. **番号で報告する。**「08 は 2 / 12 でした」で通じる
2. **実測値には、画面や記録の文言をそのまま**入れる（要約しない）
3. **落ちたら、その番号で issue を立てる**（題に番号を入れる）

## 区分

| 区分 | 何のため |
|---|---|
| **2 台** | ここが崩れたら、3 台は測れない |
| **3 台** | **いま止まっている所。**08 / 09 / 10 が本題（#28） |
| **再入室** | 版を上げるたびに要る手間（#22 / #23） |
| **リンク** | `warifu://` の道（#25 / D79） |
| **Windows** | **ASUS しか測れない**（ファイアウォールの分岐） |
| **画面** | 人が読めるか（#19） |
| **エージェント** | 人を介さない往復（#26 / issues/016 #12） |
| **網越え** | **実測 0 件**（#5） |

## いまの状態（2026-09-15 時点・**こちらが見た値**）

**シートの「判定」は空のままにしてあります。**以下は参考です ——
**人が見て書き入れるまで、合格とは書きません。**

- 01〜05: 通っている（3 台とも）
- 06: mac ⇄ mac は通った。**Windows ⇄ mac は未測**（主催にカメラが無いため）
- 07: `3 / 12`
- **08: `2 / 12`（落ちている）→ #28**
- 09 / 10: **未測**（08 が落ちているので成立しない）
- 11: `捨てた` 1〜2 回（0.1.5 は 177 回）
- 12: **出ていない**（3 台目が入っていないため未成立）
- 18〜20: 通っている（ASUS が実測）
