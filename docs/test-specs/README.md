# 検証シートの読み方（**叩き台**・2026-09-25）

> **オーナーの拒否権を残すために、叩き台として置く。**違うと思ったら、この文書ごと捨ててよい。
> シートそのもの（`001-3台のルーム.tsv`）は**一切書き換えていない**。

## なぜ要るのか

**47 行のうち、判定が入っているのは 5 行**（2026-09-25 現在）。
**止まって見えるが、止まっているのは一部だけ**である ——
**人の指が要る行と、機械だけで測れる行が、同じ列に混ざっている。**

**混ざっているので、後者まで止まって見える。**

## 3 つに分ける

### A. **機械だけで測れる**（記録・`room_status`・置き場所のファイル）

```
00-4 置き場所が読める        14   置き場所が読める（再入室）
02   経路が direct になる     03   経路が揺れない
04   主催→ゲストの文字        05   ゲスト→主催の文字
07   主催から見た名簿         08   ゲストから見た名簿
09   ゲスト同士の文字         11   紹介の嵐が戻っていない
12   戸口が名簿で通している   23   許可の言葉が部屋へ流れない
26   主催が合言葉を作る       27   入った人へ渡る
28   ゲストが受け取る         30   名簿が 3 台で揃う
31   ゲスト同士の文字が届く   33   嵐が戻っていない
34   連絡帳に載らない         37   抜けたら落ちる
18   規則なし → ふさがりを言う（**ASUS だけ**）
19   規則あり → 言わない（**ASUS だけ**）
20   調べられない → 理由を出す（**ASUS だけ**）
```

**22 行。**——**このうち、今日測れたのは 04 / 05 / 02（片側）だけ**である。

### B. **人の指が 1 回要る**（押す・貼る・回線を変える・機械を落とす）

```
01   入室できる（鍵を貼る）        13   版を上げても戻れる（入れ替えて起動）
15   入り直しで通話が作り直される  16   冷起動で問いが出る（リンクを押す）
17   勝手に入らない                21   同じ人が 1 つの顔で出る（**画面を見る**）
22   折り返しがぶら下がる（**画面を見る**）
24   エージェント同士が往復する（**押すのは人の指**と決めてある）
24-2 同上（**合成クリックが通らない機械で**）
25   別の回線から入る              29   ゲスト同士が繋がる（3 人そろえる）
35   主催を落としても残る          36   主催が落ちて戻っても合言葉が残る
41   要らない時に案内が出ない（別の回線）
```

**15 行。**

### C. **カメラ／マイクの実物が要る**（＋人の指）

```
00-1 自分のカメラが自分の画面に映る    00-2 自分のマイクが拾えている
00-3 カメラが無い機械でも入れる（**mini**）
06   映像が乗る        05-2 音が通る
10   ゲスト同士の映像  32   ゲスト同士の映像（合言葉ごし）
38   押す前に音が流れない（**2 段。1 段目は済・2 段目が指**）
39   機器を掴んでいると、そう言う（**2 段**）
40   危ない時に案内が出る
```

**10 行。**——**mini にはカメラが無い**ので、**C はほぼ Mac Air の行**である。

## いま止まっている所（**実測**）

```
判定が入っている  5 行（06 / 05-2 / 29 / 30 / 39）—— **どれも 2026-09-24 より前**
今日埋まった行    **0**
```

**A の 22 行は、3 台が同じ版で繋がっていれば、人を待たずに埋まる。**
**止まっているのは B と C であって、A ではない。**

## 次の 1 手（**推し**）

```
1  3 台を v0.1.19 に揃えて繋ぐ（**人の手が 3 回**）
2  **A の 22 行を、機械で一気に測る**（記録と room_status を読むだけ）
3  C の 38 / 39 を、Mac Air で **指 1 回**（押す前と押したあとを並べる）
4  B は、そのついでに押せるものから
```

**2 を先にやると、シートの見え方が変わる**（5 行 → 20 行台）。
**「進んでいない」のではなく「測れる状態に無かった」**が、そこではっきりする。
