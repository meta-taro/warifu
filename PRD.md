# AI-Native Trust Network — PRD（叩き）

- 原案 v1: [`docs/原案/00-原案-ai-native-p2p-trust-network.md`](docs/原案/00-原案-ai-native-p2p-trust-network.md)（2026-08-15 受領・原文そのまま保存）
- **企画書 v2: [`docs/原案/00b-企画書v2-2026-08-16.md`](docs/原案/00b-企画書v2-2026-08-16.md)（2026-08-16 受領）。差分の扱いは §11**
- この PRD は原案を**置き換えない**。原案の骨格（§0〜§23）を前提に、**叩きとして足りない所だけ**を書く
- 未決事項は `decisions.md`、着手順は `roadmap.md`、このリポジトリへ渡す作業は `issues/`

> **v2 を読む順序**: §11 を先に読むと、v2 のどこを取り込み、どこを後ろへ送ったかが分かる。
> §1〜§10 は v1 時点の記述で、**v2 でも結論は変わっていない**（変わった箇所には §11 から印を打った）。

---

## 0-a. 発端（2026-08-15 オーナー補足で判明・**ここを取り違えていた**）

この企画の発端は「世界規格を立てる」ではなく、
**「md-business に入れるより、単独 OSS にしたほうがいい」と言われたので立てた**、である。

**「md-business に入れない」は正しい。**理由は 3 つあり、どれも決定的:

- **ライフサイクルが違う。**md-business は文書ツール。通信・鍵・Identity を抱えると、
  文書を直すたびに鍵の互換性を気にすることになる
- **脅威モデルが違う。**通信基盤は攻撃対象になる。md-business にそれを持ち込むと、
  **文書を開くだけの人が通信ソフトの脆弱性を背負う**
- **ライセンスの論点が違う**（decisions.md D4）

**ただし「単独 OSS にする」と「世界規格を立てる」の間には大きな開きがある。**
原案は §19 で Public RFC Process / Multiple Implementations / Vendor-neutral Governance まで
書いてあるため、こちらは**規格として**読んだ。以下 §0-b はその読みで書いてある。
**実際の意図が「部品として独立させる」ならば、必要な作業は下記よりかなり小さい**（§0-c）。

---

## 0-b. 規格として読んだ場合に何が起きるか

**これは製品ではなく規格である。**

これまでの案件（md-business / dbboard / 連動くん / 各 store-*）は、
**1 人が 1 台で使っても価値が出る**製品だった。だから普及が遅くても死なない。
使う人が 1 人でも、その 1 人には便利なままだからである。

Trust Network は違う。**相手がいなければ価値がゼロになる。**
そして規格は、機能が足りなくて死ぬのではなく、**2 つ目の実装が現れずに死ぬ**。
「自社しか実装していない Open Specification」は、Open ではなく単なる自社仕様であり、
原案 §19 が掲げる Vendor-neutral Governance は成立しない。

したがって叩きの中心を、機能ではなく次の 3 つに置いた。

| | 問い | 置き場 |
|---|---|---|
| **① 2 実装目** | 誰が 2 つ目の実装を書くのか。書かれなければ何をもって失敗と判定するか | decisions.md **D1** |
| **② n=2 の価値** | 相手が 1 人もいない日に、誰が何のために使うのか | 本 PRD §2 / decisions.md **D2** |
| **③ 既存規格との差分** | DIDComm / DID / VC / UCAN / MLS で足りるなら、作るのは規格でなく**プロファイル**である | issues/001 |

原案 §13 の Phase 0（Specification First）は、この 3 つに対する回答として正しい。
**叩き側もコードを増やす方向には振らない。**

---

## 0-c. 「部品として独立させる」だけなら、必要なのはこれだけ

発端（§0-a）どおり、まず単独 OSS の**部品**として立てるなら、規模はこうなる。

| | 規格として立てる場合 | 部品として立てる場合 |
|---|---|---|
| 成否 | **2 つ目の実装が現れるか**（S1〜S3・D1） | **自分たちが使うか**。md-business / 連動くん / 手元の Agent が使えば成立 |
| 最初の成果物 | Phase 0 仕様書一式（原案 §13 / §17） | **§2 の n=2 プロトタイプ**（自分の PC ⇄ 自分のスマホ） |
| §19 ガバナンス | 必要（RFC プロセス・Vendor-neutral） | **今は不要**。後から名乗れる |
| D1（2 実装目） | **最優先の未決事項** | **保留でよい**。部品に 2 実装目は要らない |
| 着手前に要るもの | D1 の判定基準・D2・D6 | **D2（鍵の復旧）と D6（名前）だけ** |

**この 2 つは対立しない。**部品として先に立てて、後から規格を名乗ることはできる。
逆（規格を名乗ってから中身を作る）は、名乗った時点から
「2 実装目が来ない」が始まってしまうので不利。

**したがって叩きの既定を「部品として立てる」に置く。**
規格としての論点（§3 の S1〜S3・D1）は**取り下げず、名乗るときに使う形で残す**。
以下 §1 以降は両方に共通する内容で、規格側にしか効かない箇所には印を付けてある。

---

## 1. 何を作るか（原案の要約・再定義しない）

原案 §16 のとおり、目標は **Another Messenger ではなく
Open Trust and Communication Layer for Humans, Devices and AI Agents**。

中心は **Identity / Trust / Permission / Agent**。Text / Voice / Video / File は手段。
設計原則（§3）・Identity モデル（§4）・Trust Graph（§5）・Trust Evidence（§6）・
Connection Policy（§7）・Transport Adapter（§8）・Agent-to-Agent（§9）・
Organization（§10）・Audit（§11）・Threat Model（§14）・
AI Safety Boundary（§15）・最初に作らないもの（§21）は**原案を正本とする**。

以下、**原案に書かれていない所だけ**を書く。

---

## 2. 最初のユーザーは「他人がいない人」

原案 §20 の成功条件 1 は「2 つの Device が中央アカウントなしで Identity を確立できる」。
これは**同一人物の 2 台でよい**。

> 自分の PC の Agent と、自分のスマホの Agent が、
> **どのクラウドのアカウントも作らずに**、安全につながって仕事を渡し合える。

原案はこれを**検証条件**として書いているが、**これが最初の製品である**。

**なぜここを製品と呼ぶか。**

- **今日から価値が出る。**相手が 1 人もいなくてよい。ネットワーク効果を待たない
- **実際に困っている。**現状、自分の PC の AI に自分のスマホの状態を触らせるには、
  だいたいどこかのクラウドのアカウントを経由している
- **Trust の全要素が最小構成で出る。**Identity・Device Key・Capability・Revoke（端末紛失）が
  他人を 1 人も登場させずに揃う。**Trust Graph だけが後に残る**（自分 1 人にはグラフが無い）
- **他人が増えたときに作り直しにならない。**同じ Identity にそのまま Connection が生える

**この形なら、普及が遅くても死なない。**n=1 で価値が出る製品と、n=2 以上でしか価値が出ない規格を、
**同じリポの中で層として分ける**。前者を先に立て、後者はその上に載せる。

---

## 3. 規格として何を確定させれば「作った」と言えるか（**規格を名乗る場合のみ**）

> §0-c のとおり、部品として立てる間はこの節は**発火しない**。
> 「規格」「Open Specification」「Vendor-neutral」と名乗り始めた時点から適用する。

原案 §20 の成功条件 8 項目は、**すべて自分たちの実装だけで達成できてしまう。**
外部を必要とする条件が 1 つも無い。8 個全部 ✓ が付いても、規格としては何も証明できていない。

**成功条件を 2 段に分ける。**

### 3.1 自己検証条件（原案 §20 の 8 項目）
そのまま採用。ただし**これは「実装が動いた」の証明であって「規格が成立した」の証明ではない**と明記する。

### 3.2 規格成立条件（**原案に無い・ここが本 PRD の主張**）

| # | 条件 | なぜ必要か |
|---|---|---|
| **S1** | **仕様書だけを読んだ第三者が、こちらのコードを読まずに相互接続する実装を書ける** | 規格の定義そのもの。1 実装しか無い間、仕様は「実装の説明文」でしかない |
| **S2** | **2 つの独立実装間で Identity 確立 → Connection → E2EE Text が通る** | S1 の実測。**通らない箇所こそが仕様の穴**であり、自社実装同士では永久に見つからない |
| **S3** | **仕様に対する外部からの異議が 1 件以上出て、それを反映または明示的に却下した記録が残る** | 原案 §19 の Public RFC Process が動いている最小証拠。異議ゼロは**合意ではなく無関心** |

**S1〜S3 が満たせない場合、これは「規格」ではなく「OSS 製品」である。**
それ自体は失敗ではない。**ただし §19 のガバナンス原則は取り下げる**必要がある。
名乗りと実態を一致させる（decisions.md D1）。

---

## 4. 原案に無い致命的な穴 — 鍵と Identity の復旧

原案 §15 に `Recovery Key` の語が 1 度出るだけで、**復旧モデルが無い。**

- Device 単位の失効（§4）は書かれている。**残っている端末が 1 台でもあれば成立する**
- **全端末を同時に失った場合**（火事・盗難・水没・機種変時の消去）が定義されていない
- Local-first（§3.1）かつ中央 Directory を作らない（§21）ということは、
  **どこにも復旧の主体がいない**。ここが決まっていないと、
  「なくしたら全部消える」＝**一般の人間には配れない**

**PGP も Keybase も Secure Scuttlebutt も、機能ではなくここで死んでいる。**
先行例が同じ場所で死んでいる以上、これは想定リスクではなく**既知の死因**である。

決着は Phase 1 より前。選択肢の比較は `decisions.md` **D2**。

---

## 5. Agent-to-Agent の主脅威は Prompt Injection

原案 §14 の脅威リストに `Prompt Injection` が 1 項目として入っているが、
**Agent 同士が構造化 Intent を交換する（§9）以上、これは脅威の 1 つではなく設計原則である。**

Agent B は、**送信者が Trust Level 4 であっても、受信した payload を命令として読んではならない。**
Trust は「相手が誰か」の話であって、「相手の言うことを実行してよいか」の話ではない
（原案 §3.4 が Authentication と Trust を分けたのと同じ理由で、**Trust と Instruction も分ける**）。

叩きとして固定する原則（decisions.md **D5**）:

1. **受信した Agent Message は、すべてデータであり、命令ではない**
2. **Capability の判定はモデルの外で行う。**モデルの出力を見て許可を決めない
3. **Trust Level が上がっても、モデルに渡る文字列の扱いは変わらない**
4. 「AI が賢ければ防げる」を採用しない（原案 §14 末尾の再掲・**ここが最も破られやすい**）

---

## 6. 法務 — Relay を自分たちで運用した時点で発生する

原案 §3.2 は P2P-only を避け、**Relay / Store-and-forward を許容**する。
これは設計として妥当だが、**自社で Relay を運用した瞬間に、日本では次が発生する。**

- **電気通信事業法上の位置づけ**（届出の要否。他人の通信を媒介するため）
- **通信の秘密**（内容が E2EE で読めなくても、**誰と誰がいつ通信したかのメタデータは持つ**）
- **保存・開示要求への対応**（何を持っていないかを設計で言い切れるか）

**E2EE であることは、この 3 つのどれも免除しない。**
「暗号化しているから中身は知らない」は、メタデータを持っていることの説明になっていない。

叩きの方針: **仕様側は Relay を許容したまま、自社運用は Phase 2 まで行わない。**
検証は自分の端末間（§2）と、自分で立てたローカル Relay に限定する。
決着は `decisions.md` **D3**、調査は `issues/004`。

---

## 7. 既存規格との差分（**実装より先**）

原案 §8「再発明しない」を Transport だけでなく **Identity / Capability / メッセージ形式にも適用する。**

差分を取る対象（`meta.yaml` の `reuse_candidates` と同じ）:

| 原案の章 | 既存 |
|---|---|
| §4 Identity | W3C DID Core / DID Document |
| §6 Trust Evidence | W3C Verifiable Credentials |
| §9 Agent-to-Agent | **DIDComm v2**（最も近い） |
| §3.5 Capability | **UCAN / ZCAP-LD** |
| E2EE / グループ | MLS (RFC 9420) |
| §3.2 鍵=Identity・Relay 許容 | **Nostr**（思想が最も近い先行実装） |
| §8 Adapter 対象 | Matrix / libp2p / Iroh / Veilid |
| §6 の集約 | **Keybase（買収により停止した先行例）** |

**結論が「DIDComm v2 + UCAN でおおむね足りる」でも、それは失敗ではない。**
その場合この企画は**新規格ではなくプロファイル + Reference Implementation** になり、
**S1〜S3 の難度が一段下がる**（既存規格の実装者が 2 実装目の候補になる）。
むしろ有利な結論なので、**先に取りに行く**。調査は `issues/001`。

---

## 8. スコープ（原案 §22 の 90 日を、着手可能な量に絞る）

オーナーの受領時発言:「企画が先行して AI を擁しても生み出すペースが追いつけません」。
**したがって Month 1〜3 を全部やる前提では組まない。**

**最初にやるのはこれだけ。**

1. `issues/001` 既存規格との差分（**これが 2〜4 の前提。先に出す**）
2. `decisions.md` **D1 / D2** の決着（2 実装目・鍵復旧）
3. Phase 0 の仕様公開（原案 §13 Phase 0 の 8 項目・原案 §17 のリポ構成）
4. **§2 の n=2 プロトタイプ**（自分の PC ⇄ 自分のスマホ。他人を登場させない）

Email / Telephone / Organization / Audit は**着手しない**（原案 §13 と同じ）。
Organization（§10）と Audit（§11）は**仕様にも今は書かない** — 企業向けの記述は
早すぎると Personal 側の設計を歪める。原案の記述は将来の意図として保持する。

> **2026-08-17 変更**: **Voice / Video はこの「着手しない」から外れた**（`decisions.md` **D9**）。
> 上の 4 項目の**後ろ**に、5 番目として **4 人までの会議**（`issues/005`）と
> **md-business との掛け合わせ**（`issues/006`）が付く。§11-3 も併せて更新済み。
> **5 人以上（SFU）は着手しない。**§11-4 の D7 が未決のため。

---

## 9. OSS としての運び方

- **public リポ・初回 commit から公開作法**。
  commit history に個人名・個人メールを残さない。**検出 CI を初回 commit に同梱する**
- **Spec と Reference Implementation でライセンスを分ける**（decisions.md **D4**）。
  原案 §18 は 1 つ選ぶ前提だが、**規格に AGPL を掛けると、規格が最も必要とする
  企業実装を自分で締め出す**。Spec は広く（CC / 寛容）、実装は別に決める
- **名前が入手経路そのもの**（decisions.md **D6**）。OSS では検索して辿り着けない名前は存在しないのと同じ

---

## 10. この叩きで意図的に決めていないこと

- ~~実装言語~~ → **2026-08-17 に決定（`decisions.md` D10）**: コアは **Rust**、Transport は **iroh 1.0**、
  デスクトップは **Tauri 2 + TypeScript + SvelteKit + pnpm**、メディアは **WebRTC**（WebView 側）
- ~~Transport の第一実装~~ → 同上
- Trust Level 0〜5 の Capability 割り当て（原案 §5 の初期例のまま。プリセットは実装後に調整）
- 製品名（D6）

> **決めた理由**: `issues/001` の結論を待たずに決めたのではなく、**会議に関わる範囲だけ先に結論を出した**
> （`roadmap.md` Step 1 の追記）。Identity / VC / Capability の差分調査は並行して続く。
> **止めるべき条件は D10 に書いてある。**残りの調査が別の言語を指した場合は、そこで戻る。

---

## 11. 企画書 v2（2026-08-16 受領）の差分をどう扱うか

v2 は v1 の**置き換えではなく拡張**。骨格（Identity / Trust / Permission / Agent が中心・
Another Messenger ではない・再発明しない）は v1 と完全に同一で、**下位の判断が増えている**。

**したがって §1〜§10 の結論は変えない。**v2 の追加分だけを以下 4 つに振り分ける。

| 扱い | v2 の該当章 | 理由 |
|---|---|---|
| **そのまま取り込む** | §4 既存 OSS 調査 / §16 Intent 名前空間 / §20 法務 / §22 再発明しない | 既存の結論を**補強する**方向の追加 |
| **前倒しする** | §8 Personal / Work Profile | 後から入れると Identity の形が変わる。§2 の n=2 と両立する |
| **後ろへ送る** | §13 §14 Voice / Video / Embedded SFU | **§2 の n=2 では発火しない**。かつ v2 §22「再発明しない領域」そのもの |
| **新しい未決事項にする** | §14 の「利用者の端末が中継する」構造 | D3 の想定範囲を**超えている**（→ `decisions.md` **D7**） |

---

### 11-1. そのまま取り込むもの

**① §4 の既存 OSS 調査（具体名が入った）**

v1 は「既存 OSS を使う」としか書いていなかったが、v2 は 7 件を名指しした。
このうち `issues/001` の調査対象に**未登録だったもの**を追加する:

| v2 で新出 | 突き合わせる原案の章 | なぜ効くか |
|---|---|---|
| **SimpleX Chat** | §7 Connection Policy / §3.1 Local-first | **招待型 Connection と「利用者 ID を持たない」設計**が §7 とほぼ同じ。Spam 耐性の先行実装 |
| **Jami** | §8 Transport / §13 P2P Voice/Video | 分散 P2P で Voice/Video/Conference を**実際に動かしている**数少ない例 |
| **OpenMolt Network** | §9 Agent-to-Agent / §8 Transport | **P2P Agent-to-Agent + libp2p + DID** ＝ v2 §4 の言うとおり「今回にかなり近い」。**Building Block 候補の最有力** |
| **Agent Identity Protocol (AIP)** | §4 Agent Identity / §3.5 Capability | **Capability ベース委譲の標準化提案**。UCAN / ZCAP と並べて読む |
| **Signet AI** | §15 Persistent Agent Identity | Local-first Agent の Memory / Secrets。**v2 §1 の「Persistent Agent Identity」に最も近い** |
| **OpenAgents** | §9 / §10 | Human + Agent 協働。ただし**Workspace 中心＝こちらとは中心が違う**（v2 §4 が自分で書いている） |

**OpenMolt / AIP / Signet の 3 つは、`issues/001` の「該当なし」判定を大きく減らす可能性がある。**
減れば減るほど良い（§7 のとおり、既存に寄せられるほど 2 実装目が現実的になる）。

**② §20 の法務（Winny の教訓）**

v1 の `decisions.md` **D3** が指摘していた論点を、**企画側が自分から取り込んだ**。
「Public Piracy File Index を作らない」「Unlimited Anonymous DM を作らない」
「違法利用を価値とする機能を作らない」は、**非スコープとして明文化された**＝ D3 の前半が固まった。

**ただし D3 が消えたわけではない**。むしろ §14 が新しい穴を開けた（→ 11-4）。

**③ §16 の Intent 名前空間**

`message.* / meeting.* / calendar.* / file.* / contact.* / task.* / quotation.* / invoice.* / notification.*`。
v1 は quotation の例が 1 つあるだけだった。**Phase 3（Agent）の仕様の材料になる。**
ただし `invoice.*` / `quotation.*` は md-business の領域と重なるので、
**先に定義するのは `meeting.*` と `calendar.*` だけでよい**（v2 §17 の代表 Demo がそこだから）。

---

### 11-2. 前倒しするもの — Personal / Work Profile（v2 §8）

**v2 の追加のうち、唯一「着手を早める」方向に効くもの。**

§2 の n=2（自分の PC ⇄ 自分のスマホ）は、構造的に
**「Personal Profile に Device が 2 台ぶら下がっている」形**そのもの。
Profile の器を最初から持たせておけば、Work Profile は後から**足すだけ**で済む。

**逆に、後から入れると Identity の形が変わる。**
Profile ごとに Identity / Keys / Trust Graph / Agent Memory を Isolation するということは、
**鍵の持ち方が Profile 単位になる**ということで、これは D2（鍵の復旧）と同じ性質の変更にあたる。
Phase 1 を書き始めた後に入れると作り直しになる。

**したがって Phase 1 に「Profile の器」を含める。**
ただし**含めるのは器だけ**で、v2 §18 の Organization Policy（Member / ACL / Retention / Audit）は
§8 のとおり**今回書かない**。企業向けの記述が Personal 側の設計を歪めるという理由は v2 でも変わらない。

---

### 11-3. 後ろへ送るもの — ~~Voice / Video /~~ Embedded SFU（v2 §13 §14 / Phase 3・4・7）

> **2026-08-17 変更（`decisions.md` D9）。この節は半分が覆った。**
>
> | | 2026-08-16 の判断 | 現在 |
> |---|---|---|
> | **Voice / Video（4 人まで）** | 後段へ送る | **前倒し。**`roadmap.md` Step 3.5 / `issues/005` |
> | **Embedded SFU（5 人以上）** | 最後尾 | **最後尾のまま。**動かしていない |
>
> **覆った理由**は下の議論が間違っていたからではない。**D8 が自分で置いた例外条項が発火した**ため。
> D8 は「覆してよい条件: Voice/Video を先に出す事業上の理由が出た場合。
> その場合も Embedded SFU は切り離して最後尾に残す（D7 が未決のため）」と書いていた。
> 2026-08-17 のオーナー指示（Google Workspace / Microsoft Teams・Office の置き換え）が
> これに当たる。**但し書きも含めてそのまま守っている。**
>
> **以下の議論は、Embedded SFU についてはいまも有効。**
> 「§22 再発明しない領域に費やしてから独自価値に着手する順になっている」という指摘は、
> Voice/Video 側では **WebRTC をそのまま使い、Codec を一切書かない**ことで回避している
> （`issues/005` の満たすこと 3・4）。**再発明していないので、この批判は当たらない。**
> SFU 側は orchestration が独自になるため回避できず、加えて **D7 が未決**。だから残す。

**v2 の Phase 順は、v2 自身の §22 / §23 と矛盾している。**

v2 は §22 で「再発明しない領域」を挙げ、§23 で「独自価値を出す領域」を 13 個挙げている。
その 2 つのリストに Phase 番号を重ねると、こうなる:

| v2 Phase | 内容 | v2 自身の分類 |
|---|---|---|
| 3 | Voice / Video / WebRTC / TURN | **§22 再発明しない**（Codec・WebRTC・NAT traversal・TURN が全部そこに載っている） |
| 4 | Embedded SFU | **§22 再発明しない**（SFU engine がそこに載っている）＋ §23-9 の orchestration だけが独自 |
| **5** | **Agent Identity / Capability / A2A / Calendar Demo** | **§23-1・5・6・11・12 ＝ 独自価値の中核** |

**再発明しない領域に 2 フェーズ費やしてから、独自価値に着手する順になっている。**

さらに **v2 の中で矛盾がある**。§26「初期 90 日」は Month 3 に
「Agent Identity / Capability / A2A / Calendar Adapter / 双方 Calendar Event 作成 Demo」を置いている。
これは **Phase 5 の中身**であり、**Phase 3・4 を飛ばさないと 90 日で到達しない**。

**90 日プラン（§26）のほうが正しい。**v1 の Phase 順（Phase 3 = Agent）と一致しており、
§23 の独自価値を先に取りに行く形になっている。

**したがって Phase 順は v1 のまま維持し、~~v2 の Voice/Video/SFU は~~ Embedded SFU は後段へ送る**（`roadmap.md` 参照）。
v2 §13 §14 の**設計内容そのものは捨てない**。Embedded SFU / Cooperative Relay の
Peer Election・Health Metrics・Failover・5 段 Fallback は、**将来の設計として保持する**
（→ `decisions.md` **D8**）。

**併せて、n=2 の観点からも同じ結論になる。**
Embedded SFU は「大人数会議で SFU が必要になるから、余裕のある Desktop をそれにする」機能で、
**定義上 n=2 では一度も発火しない**。企画中で技術難度が最も高く、
かつ**最初の利用者にとっての価値がゼロ**という組み合わせになっている。

---

### 11-4. 新しい未決事項 — 「利用者の端末が他人の通信を中継する」（v2 §14）

**これが v2 で最も重い追加。**`decisions.md` に **D7** として立てた。

v1 の **D3** は「**自分たちが** Relay を運用したときの法的位置づけ」を問うていた。
暫定方針は「自社での Relay 運用は Phase 2 まで行わない」。

**v2 §14 は、中継する主体を自社から利用者に移している。**

> Desktop App 自身が必要に応じて SFU / Relay Node になる（v2 §14）

**この一文で D3 の暫定方針が効かなくなる。**自社が運用しないので「行わない」が成立しない一方、
**他人の通信を媒介する行為は発生している**。発生させているのは利用者の PC で、
その状態を作ったのはこちらの設計である。

論点は 3 つ:

1. **電気通信事業法** — 他人の通信を媒介するのは電気通信役務にあたる。
   これを**利用者の端末が既定で行う**設計にした場合、事業者性が誰に生じるのかが未整理。
   D3 は「自社が届出をするか」の問いだったが、D7 は**利用者に何を負わせるか**の問いになる
2. **通信の秘密** — E2EE で中身は読めないが、**中継者は「誰と誰がいつ」を必然的に持つ**。
   §11-4 の主体は利用者なので、**利用者が第三者の通信のメタデータに触れる**
3. **v2 §20 が挙げた Winny の教訓が、実は §14 に最も強く当たる** —
   v2 は「Public Piracy File Index を作らない」で回避したと書いているが、
   Winny で争点になったのは index の有無ではなく、
   **「利用者の PC が、本人が中身を知らないまま他人のデータを運ぶ構造を、開発者が提供したこと」**である。
   §14 は index を持たない代わりに **E2EE で中身が見えない**ので、
   **中継している当人にも運営にも、何が流れているか原理的に分からない**。
   「知らなかった」は成立するが、同時に**止める手段も無い**という状態になる

**v2 §14 は既に一つ答えを持っている。**Relay Capability に **`Willingness`** を含めている＝
**利用者の同意を取る前提**になっている。**この同意の取り方が、そのまま法的な立て付けになる。**
既定 ON か既定 OFF か、何を中継しているか本人に見えるか、いつでも止められるか。
**ここは技術判断ではなく事業判断なので、オーナーが決める**（D7）。

**叩きとしての暫定方針**（覆してよい）:

- **§14 の Embedded SFU / Cooperative Relay は、設計として保持し、実装順は最後尾に置く**
- **他人の通信を中継する機能を、既定 ON にしない**
- n=2 と Phase 1〜3 では**そもそも中継が発生しない**ので、**D7 の決着を待たずに着手できる**
- 調査は `issues/004`（D3 と D7 をまとめて扱う）

---

### 11-5. v2 でも直っていないもの — 成功条件が全部「自己完結」のまま

v1 の PRD §3 / D1 で指摘した点:

> 原案 §20 の成功条件 8 項目は、**すべて自分たちの実装だけで達成できてしまう。**
> 外部を必要とする条件が 1 つも無い。

**v2 §28 で成功条件は 8 → 14 個に増えたが、増えた 6 個も全部自己完結している。**
「Desktop の Relay / SFU 化」も「Organization Connection Control」も、
**自社実装 1 つで ✓ が付く**。14 個全部達成しても、**規格として成立したかは依然として判定できない。**

**ただしこれは v2 の欠陥ではない。**
§0-c のとおり**既定の位置づけは「部品」**であり、部品に 2 実装目は要らない。
自己完結した成功条件は、**部品としては正しい**。

**v2 §30 の「GO」も、部品としての GO なら整合する。**
問題になるのは v2 §29 が「規格」寄りの言葉（Open Trust Layer / World）で締めている点だけで、
**名乗りを変えるまで D1 は発火しない**（§0-c）。**現時点で決める必要はない。**

---

## 12. P2P Agent Mail 企画書（2026-09-01 受領）の差分をどう扱うか

**これは v3 ではない。**「AI ネイティブ通信基盤 / MCP メーラー / P2P メール互換 OSS」として、
**別の OSS のつもりで書かれた企画書**（全 56 章）が届いた。

実測すると、**骨格は本リポジトリの roadmap Phase 3 + Phase 5 と同じもの**である。

| 企画書 | 本リポジトリに既にあるもの |
|---|---|
| §10 MCP / §21 Agent-to-Agent / §23 Tool Permission | roadmap **Phase 3**「Agent Identity / Capability / Approval Gate / A2A Intent / MCP Adapter」 |
| §15 SMTP / IMAP 互換 / §16 Postfix の位置づけ | roadmap **Phase 5**「Email / Messenger / Telephone Adapter」 |
| §7 P2P 配送 / §18 Identity / §19 マルチデバイス / §34 Federation / §35 完全 P2P にしない理由 | §1 / D10 / D12 / D13。**実装が動いている**（`warifu-core` / `warifu-intent`） |
| §23 Mail is Data, not Instruction | **D5**（そちらのほうが射程が広い・12-4） |

**別 OSS として立てない**（→ `decisions.md` **D16**）。Identity / Directory / Relay /
Multi-device / Federation は両方に要る。別に立てれば **D2 に 2 回ぶつかる**うえ、
**§2 の「相手がいないと価値がゼロ」問題を 2 つ同時に抱える**ことになる。

振り分けは以下 4 つ。

| 扱い | 該当章 | 理由 |
|---|---|---|
| **そのまま取り込む** | §11〜§14 / §38 / §39（Token Economy・Action Inbox） | **P2P も Directory も要らず n=1 で効く。**§2 とそろっている唯一の追加 |
| **原則の下へ入れる** | §24 Prompt Injection 対策 | D5 は原則、§24 は実装項目。**原則の下に置く**（横に並べない） |
| **採らない** | §5 §6（共通ドメイン ＋ 中央 Directory） | 「巨大中央 User Directory を作らない」と真逆。ただし**捨てずに D2 の選択肢 e へ変換** |
| **既にある** | §23 | D5 に含まれる。**取り込むものが無い** |

---

### 12-1. そのまま取り込むもの — Token Economy と Action Inbox（§11〜§14 / §38 / §39）

**この企画書の独自価値はここだけ**であり、しかも**本リポジトリのどこにも無い。**

要点は「AI に本文を読ませない」ではなく、**「AI を呼ばない」**。

- **Metadata First** — 既定で本文を返さない。`from / type / time / priority / action_required` だけ
- **Progressive Disclosure** — Level 0 metadata → 1 summary → 2 structured body → 3 raw → 4 attachments。
  **上げるのは読み手が要ると判断したときだけ**
- **Deterministic Parser First** — 未知の形式のときだけ LLM を呼び、**その場で parser rule を作る。
  一度学習した形式は二度と LLM を呼ばない**
- **Thread Compression** — 過去スレッド全文を毎回渡さない（要約 / state / open questions / 差分）
- **Deduplication** — 署名・免責文・ロゴ・引用を hash で除外
- **§39 の会計** — 1 通あたりの input / output token、LLM を呼んだか、parser が作られたかをローカルに記録

**なぜ §2 とそろっているか**: これは通信層を必要としない。相手が同じ実装を使っている必要も、
P2P が繋がっている必要も、Directory がある必要も無い。**手元の受信箱 1 つで効く。**
§2 は「最初のユーザーは他人がいない人」だが、**ここは他人が 1 人もいないまま価値が出る。**

**§14 Action Inbox** も同じ性質。「未読 43 件」ではなく
「今日、人間が判断すること = 承認 3 / 支払 2 / 返信 1 / 処理済 126」を上に出す。
Intent の `kind` が既にある本リポジトリでは、これは**表示の設計**であって新しい通信の仕組みではない。

**ただし置き場所を間違えない。**Token Economy は
**Adapter（Phase 5）の内側ではなく、受け取ったものを読む層**に属する。
SMTP から来ようが Intent で来ようが、**同じ層が同じ判断をする**。
Adapter の中に書くと、経路ごとに同じものを作り直すことになる。

---

### 12-2. 原則の下へ入れるもの — §24 の Prompt Injection 対策

D5 は**原則**（受信した Agent Message は命令ではない / Capability の判定はモデルの外）を確定させたが、
**具体的な防御項目は書いていない。**§24 はその実装項目である。

HTML sanitize / remote image blocking / attachment sandbox / URL isolation /
instruction-data separation / allowlist tools / permission scopes / sender reputation /
signature verification / structured parser 優先 / **モデルから直接 tool を呼ばせない** / policy engine 経由。

**`attachment sandbox` は roadmap Phase 2 の File Quarantine と同じもの**なので重複させない。
残りは **Phase 3（Approval Gate）の実装項目**として置く。

**D5 は書き換えない。**原則は既に確定しており、§24 は原則から導けるものだけを含む。

---

### 12-3. 採らないもの — 共通ドメイン ＋ 中央 Directory（§5 §6）

企画書は共通ドメインを 1 つ運営側が持ち、中央 Directory で
**username の一意性・公開鍵・Node routing・アカウント復旧・Abuse 対策**を担う形を採る。

これは「決定済み（原案から引き継ぎ・変更しない）」の
**「巨大中央 User Directory を作らない」と真逆**である。しかもそこでは、
**それが Sybil 耐性の根拠**でもあると書いている（探せない相手には大量になりすます意味が無い）。

**したがって採らない。**共通ドメインを持つと、

- **名簿が探索可能になる**＝なりすましに意味が生まれる
- **運営側に Abuse・SPF / DKIM / DMARC・domain reputation の責任が発生する**
  （企画書 §25 §26 が自分でそう書いている）
- §0-0 の思想（中央のサーバー費用を誰かが払う構造を置き換える）と逆を向く

**ただし議論としては捨てない。**企画書が持ち込んだのは**新しい問いではなく、D2 への答えの 1 つ**である。
D2（全端末を失った人間の Identity）は「復旧を頼む相手が構造上いない」ことが死因で、
**中央 Directory があれば解ける。**選択肢 **e** として `decisions.md` D2 に追記した。

**要点は「中央を置くか」ではなく「その中央が探索可能な名簿を持つか」。**
復旧だけを担い、**問い合わせても他人を探せない**形なら、Sybil 耐性を落とさずに D2 を解ける可能性がある。
これは a〜d のどれとも別の軸なので、**D2 の議論に戻す。**

---

### 12-4. 既にあるもの — §23 は D5 に含まれる

企画書 §23 は「Mail is Data, not Instruction」として、本文に命令が書かれていても
System Prompt として扱わない、untrusted input とする、と書く。

**D5 はこれを含み、さらに 2 つ先まで書いている。**
Capability の判定をモデルの外で行うこと、
**Trust Level が上がってもモデルに渡る文字列の扱いを変えないこと**。
後者が無いと「信頼を得れば命令が通る」構造が残り、Sybil に価値が生まれる。

**取り込むものは無い。**ここは既に決着している。

---

### 12-5. なぜ別 OSS に見えたか

**12-1 が本当に単独で成立するから。**Token Economy と Action Inbox は通信層を必要とせず、
既存のメール（SMTP / IMAP）に対してだけでも動く。
そこへ P2P・Identity・Directory の記述が（本リポジトリと同じ骨格で）付いてきたため、
全体が新規に見えた。

**切り分けは「P2P が要るか」で引く。**要らない部分だけが新しい。

---

**上流が作るのはここまで。**技術調査・比較・実装はこのリポジトリで行う。
